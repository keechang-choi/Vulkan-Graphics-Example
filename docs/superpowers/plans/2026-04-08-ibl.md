# IBL (Image-Based Lighting) Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** pbr 예제에 IBL ambient lighting과 skybox를 추가한다 — startup에 HDR 환경맵에서 irradiance/prefilter/BRDF LUT를 생성하고, composition 패스 ambient를 IBL로 교체, 배경 skybox를 별도 패스로 렌더링.

**Architecture:** Sascha Willems 방식: offscreen 2D image에 렌더 후 vkCmdCopyImage로 큐브맵 face에 복사. IBL 생성은 startup 1회. 매 프레임은 기존 G-buffer → Composition(IBL 포함) → Skybox → Sprite 순서. `useIBL` 단일 토글로 IBL ambient와 skybox 동시 제어.

**Tech Stack:** Vulkan-HPP RAII, VMA, glm, stb_image (stbi_loadf for HDR), GLSL 450, glslc

---

## 파일 맵

| 상태 | 경로 | 역할 |
|------|------|------|
| **수정** | `src/base/vgeu_buffer.hpp` | VgeuImage cubemap 오버로드 선언 추가 |
| **수정** | `src/base/vgeu_buffer.cpp` | VgeuImage cubemap 오버로드 구현 |
| **수정** | `src/examples/pbr/pbr.hpp` | IBL/skybox 멤버 추가 (텍스처, pipeline, descriptor, CaptureUbo) |
| **수정** | `src/examples/pbr/pbr.cpp` | prepareIBL(), prepareSkybox*(), setupDescriptors/Pipelines 수정, buildCommandBuffers 수정 |
| **생성** | `shaders/pbr/equirect.vert` | 큐브 36 vertex 하드코딩, capture UBO MVP |
| **생성** | `shaders/pbr/equirect.frag` | equirectangular → cubemap face 변환 |
| **생성** | `shaders/pbr/irradiance.frag` | 반구 적분 irradiance |
| **생성** | `shaders/pbr/prefilter.frag` | GGX importance sampling prefilter |
| **생성** | `shaders/pbr/brdf_lut.vert` | fullscreen triangle, no buffer |
| **생성** | `shaders/pbr/brdf_lut.frag` | BRDF LUT 생성 (NdotV, roughness → scale, bias) |
| **생성** | `shaders/pbr/skybox.vert` | 큐브 36 vertex, push constant view+proj, depth=1 trick |
| **생성** | `shaders/pbr/skybox.frag` | envCubemap 샘플링 + Uncharted2 tonemap |
| **수정** | `shaders/pbr/pbr.frag` | set=1 IBL 텍스처 + FresnelSchlickRoughness + IBL ambient 블록 |

---

## Task 1: VgeuImage 큐브맵 오버로드

**Files:**
- Modify: `src/base/vgeu_buffer.hpp`
- Modify: `src/base/vgeu_buffer.cpp`

- [ ] **Step 1: vgeu_buffer.hpp에 오버로드 선언 추가**

`src/base/vgeu_buffer.hpp`의 기존 VgeuImage 생성자 선언 바로 다음 줄에 추가:

```cpp
  // Cubemap variant: arrayLayers=6, VK_IMAGE_CREATE_CUBE_COMPATIBLE_BIT,
  // viewType=eCube
  VgeuImage(const vk::raii::Device& device, VmaAllocator allocator,
            vk::Format format, const vk::Extent2D& extent,
            vk::ImageTiling tiling, vk::ImageUsageFlags usage,
            vk::ImageLayout initialLayout, VmaMemoryUsage memUsage,
            VmaAllocationCreateFlags allocCreateFlags,
            vk::ImageAspectFlags aspectMask, uint32_t mipLevels,
            bool isCubemap);
```

- [ ] **Step 2: vgeu_buffer.cpp에 구현 추가**

`src/base/vgeu_buffer.cpp`의 `VgeuImage::~VgeuImage()` 바로 위에 추가:

```cpp
VgeuImage::VgeuImage(const vk::raii::Device& device, VmaAllocator allocator,
                     vk::Format format, const vk::Extent2D& extent,
                     vk::ImageTiling tiling, vk::ImageUsageFlags usage,
                     vk::ImageLayout initialLayout, VmaMemoryUsage memUsage,
                     VmaAllocationCreateFlags allocCreateFlags,
                     vk::ImageAspectFlags aspectMask, uint32_t mipLevels,
                     bool isCubemap)
    : allocator(allocator), format(format) {
  const uint32_t layers = isCubemap ? 6u : 1u;

  VkImageCreateInfo vkImageCI{};
  vkImageCI.sType = VK_STRUCTURE_TYPE_IMAGE_CREATE_INFO;
  vkImageCI.flags =
      isCubemap ? VK_IMAGE_CREATE_CUBE_COMPATIBLE_BIT : 0u;
  vkImageCI.imageType = VK_IMAGE_TYPE_2D;
  vkImageCI.format = static_cast<VkFormat>(format);
  vkImageCI.extent.width = extent.width;
  vkImageCI.extent.height = extent.height;
  vkImageCI.extent.depth = 1;
  vkImageCI.mipLevels = mipLevels;
  vkImageCI.arrayLayers = layers;
  vkImageCI.samples = VK_SAMPLE_COUNT_1_BIT;
  vkImageCI.tiling = static_cast<VkImageTiling>(tiling);
  vkImageCI.usage = static_cast<VkImageUsageFlags>(usage);
  vkImageCI.sharingMode = VK_SHARING_MODE_EXCLUSIVE;
  vkImageCI.initialLayout = static_cast<VkImageLayout>(initialLayout);

  VmaAllocationCreateInfo allocCI{};
  allocCI.usage = memUsage;
  allocCI.flags = allocCreateFlags;

  VkImage vkImage = VK_NULL_HANDLE;
  VkResult result = vmaCreateImage(allocator, &vkImageCI, &allocCI, &vkImage,
                                   &alloc, &allocInfo);
  assert(result == VK_SUCCESS && "VMA ERROR: failed to create cubemap image.");
  image = vk::Image(vkImage);

  auto viewType =
      isCubemap ? vk::ImageViewType::eCube : vk::ImageViewType::e2D;
  imageView = vk::raii::ImageView(
      device,
      vk::ImageViewCreateInfo(
          vk::ImageViewCreateFlags(), image, viewType, format, {},
          vk::ImageSubresourceRange{aspectMask, 0, mipLevels, 0, layers}));
  assert(static_cast<VkImageView>(*imageView) != VK_NULL_HANDLE);
}
```

- [ ] **Step 3: clang-format 적용**

```bash
clang-format -i src/base/vgeu_buffer.hpp src/base/vgeu_buffer.cpp
```

- [ ] **Step 4: 빌드 확인**

```bash
cmake --build ./build --config Debug 2>&1 | tail -30
```

Expected: 에러 없이 빌드 성공.

- [ ] **Step 5: 커밋**

```bash
rtk git add src/base/vgeu_buffer.hpp src/base/vgeu_buffer.cpp
rtk git commit -m "feat(vgeu): add VgeuImage cubemap constructor overload"
```

---

## Task 2: IBL 생성 셰이더 작성 + 컴파일

**Files:**
- Create: `shaders/pbr/equirect.vert`
- Create: `shaders/pbr/equirect.frag`
- Create: `shaders/pbr/irradiance.frag`
- Create: `shaders/pbr/prefilter.frag`
- Create: `shaders/pbr/brdf_lut.vert`
- Create: `shaders/pbr/brdf_lut.frag`

- [ ] **Step 1: equirect.vert 작성**

```glsl
// shaders/pbr/equirect.vert
#version 450

layout(set = 0, binding = 0) uniform CaptureUbo {
    mat4 mvp;
} capture;

layout(location = 0) out vec3 outLocalPos;

const vec3 positions[36] = vec3[](
    // +X
    vec3( 1,-1, 1),vec3( 1,-1,-1),vec3( 1, 1,-1),
    vec3( 1, 1,-1),vec3( 1, 1, 1),vec3( 1,-1, 1),
    // -X
    vec3(-1,-1,-1),vec3(-1,-1, 1),vec3(-1, 1, 1),
    vec3(-1, 1, 1),vec3(-1, 1,-1),vec3(-1,-1,-1),
    // +Y
    vec3(-1, 1, 1),vec3( 1, 1, 1),vec3( 1, 1,-1),
    vec3( 1, 1,-1),vec3(-1, 1,-1),vec3(-1, 1, 1),
    // -Y
    vec3(-1,-1,-1),vec3( 1,-1,-1),vec3( 1,-1, 1),
    vec3( 1,-1, 1),vec3(-1,-1, 1),vec3(-1,-1,-1),
    // +Z
    vec3(-1,-1, 1),vec3(-1, 1, 1),vec3( 1, 1, 1),
    vec3( 1, 1, 1),vec3( 1,-1, 1),vec3(-1,-1, 1),
    // -Z
    vec3( 1,-1,-1),vec3( 1, 1,-1),vec3(-1, 1,-1),
    vec3(-1, 1,-1),vec3(-1,-1,-1),vec3( 1,-1,-1)
);

void main() {
    outLocalPos = positions[gl_VertexIndex];
    gl_Position = capture.mvp * vec4(outLocalPos, 1.0);
}
```

- [ ] **Step 2: equirect.frag 작성**

```glsl
// shaders/pbr/equirect.frag
#version 450

layout(set = 1, binding = 0) uniform sampler2D hdrMap;

layout(location = 0) in  vec3 inLocalPos;
layout(location = 0) out vec4 outColor;

const vec2 invAtan = vec2(0.1591, 0.3183);

vec2 SampleSphericalMap(vec3 v) {
    vec2 uv = vec2(atan(v.z, v.x), asin(v.y));
    uv *= invAtan;
    uv += 0.5;
    return uv;
}

void main() {
    vec2 uv = SampleSphericalMap(normalize(inLocalPos));
    outColor = vec4(texture(hdrMap, uv).rgb, 1.0);
}
```

- [ ] **Step 3: irradiance.frag 작성**

```glsl
// shaders/pbr/irradiance.frag
#version 450

layout(set = 1, binding = 0) uniform samplerCube envMap;

layout(push_constant) uniform PushBlock {
    float deltaPhi;
    float deltaTheta;
} push;

layout(location = 0) in  vec3 inLocalPos;
layout(location = 0) out vec4 outColor;

const float PI = 3.14159265359;

void main() {
    vec3 N = normalize(inLocalPos);
    vec3 up = vec3(0.0, 1.0, 0.0);
    vec3 right = normalize(cross(up, N));
    up = normalize(cross(N, right));

    vec3 irradiance = vec3(0.0);
    float nrSamples = 0.0;

    for (float phi = 0.0; phi < 2.0 * PI; phi += push.deltaPhi) {
        for (float theta = 0.0; theta < 0.5 * PI; theta += push.deltaTheta) {
            vec3 tangentSample = vec3(
                sin(theta) * cos(phi),
                sin(theta) * sin(phi),
                cos(theta));
            vec3 sampleVec = tangentSample.x * right
                           + tangentSample.y * up
                           + tangentSample.z * N;
            irradiance += texture(envMap, sampleVec).rgb
                        * cos(theta) * sin(theta);
            nrSamples++;
        }
    }
    irradiance = PI * irradiance / nrSamples;
    outColor = vec4(irradiance, 1.0);
}
```

- [ ] **Step 4: prefilter.frag 작성**

```glsl
// shaders/pbr/prefilter.frag
#version 450

layout(set = 1, binding = 0) uniform samplerCube envMap;

layout(push_constant) uniform PushBlock {
    float roughness;
    uint  numSamples;
} push;

layout(location = 0) in  vec3 inLocalPos;
layout(location = 0) out vec4 outColor;

const float PI = 3.14159265359;

float RadicalInverse_VdC(uint bits) {
    bits = (bits << 16u) | (bits >> 16u);
    bits = ((bits & 0x55555555u) << 1u) | ((bits & 0xAAAAAAAAu) >> 1u);
    bits = ((bits & 0x33333333u) << 2u) | ((bits & 0xCCCCCCCCu) >> 2u);
    bits = ((bits & 0x0F0F0F0Fu) << 4u) | ((bits & 0xF0F0F0F0u) >> 4u);
    bits = ((bits & 0x00FF00FFu) << 8u) | ((bits & 0xFF00FF00u) >> 8u);
    return float(bits) * 2.3283064365386963e-10;
}

vec2 Hammersley(uint i, uint N) {
    return vec2(float(i) / float(N), RadicalInverse_VdC(i));
}

vec3 ImportanceSampleGGX(vec2 Xi, vec3 N, float roughness) {
    float a = roughness * roughness;
    float phi = 2.0 * PI * Xi.x;
    float cosTheta = sqrt((1.0 - Xi.y) / (1.0 + (a * a - 1.0) * Xi.y));
    float sinTheta = sqrt(1.0 - cosTheta * cosTheta);
    vec3 H = vec3(cos(phi) * sinTheta, sin(phi) * sinTheta, cosTheta);
    vec3 up = abs(N.z) < 0.999 ? vec3(0, 0, 1) : vec3(1, 0, 0);
    vec3 tangent = normalize(cross(up, N));
    vec3 bitangent = cross(N, tangent);
    return normalize(tangent * H.x + bitangent * H.y + N * H.z);
}

float DistributionGGX(float NdotH, float roughness) {
    float a  = roughness * roughness;
    float a2 = a * a;
    float denom = (NdotH * NdotH * (a2 - 1.0) + 1.0);
    return a2 / (PI * denom * denom);
}

void main() {
    vec3 N = normalize(inLocalPos);
    vec3 R = N;
    vec3 V = R;

    vec3  prefilteredColor = vec3(0.0);
    float totalWeight = 0.0;
    float envResolution = 512.0;

    for (uint i = 0u; i < push.numSamples; ++i) {
        vec2 Xi = Hammersley(i, push.numSamples);
        vec3 H  = ImportanceSampleGGX(Xi, N, push.roughness);
        vec3 L  = normalize(2.0 * dot(V, H) * H - V);

        float NdotL = max(dot(N, L), 0.0);
        if (NdotL > 0.0) {
            float NdotH = max(dot(N, H), 0.0);
            float HdotV = max(dot(H, V), 0.0);
            float D = DistributionGGX(NdotH, push.roughness);
            float pdf = (D * NdotH / (4.0 * HdotV)) + 0.0001;
            float saTexel  = 4.0 * PI / (6.0 * envResolution * envResolution);
            float saSample = 1.0 / (float(push.numSamples) * pdf + 0.0001);
            float mipLevel = push.roughness == 0.0
                ? 0.0 : 0.5 * log2(saSample / saTexel);
            prefilteredColor +=
                textureLod(envMap, L, mipLevel).rgb * NdotL;
            totalWeight += NdotL;
        }
    }
    outColor = vec4(prefilteredColor / totalWeight, 1.0);
}
```

- [ ] **Step 5: brdf_lut.vert 작성**

```glsl
// shaders/pbr/brdf_lut.vert
#version 450

layout(location = 0) out vec2 outUV;

void main() {
    // Fullscreen triangle: gl_VertexIndex 0,1,2
    outUV = vec2((gl_VertexIndex << 1) & 2, gl_VertexIndex & 2);
    gl_Position = vec4(outUV * 2.0 - 1.0, 0.0, 1.0);
}
```

- [ ] **Step 6: brdf_lut.frag 작성**

```glsl
// shaders/pbr/brdf_lut.frag
#version 450

layout(location = 0) in  vec2 inUV;
layout(location = 0) out vec2 outColor;  // RG: scale, bias

const float PI = 3.14159265359;

float RadicalInverse_VdC(uint bits) {
    bits = (bits << 16u) | (bits >> 16u);
    bits = ((bits & 0x55555555u) << 1u) | ((bits & 0xAAAAAAAAu) >> 1u);
    bits = ((bits & 0x33333333u) << 2u) | ((bits & 0xCCCCCCCCu) >> 2u);
    bits = ((bits & 0x0F0F0F0Fu) << 4u) | ((bits & 0xF0F0F0F0u) >> 4u);
    bits = ((bits & 0x00FF00FFu) << 8u) | ((bits & 0xFF00FF00u) >> 8u);
    return float(bits) * 2.3283064365386963e-10;
}
vec2 Hammersley(uint i, uint N) {
    return vec2(float(i) / float(N), RadicalInverse_VdC(i));
}

vec3 ImportanceSampleGGX(vec2 Xi, vec3 N, float roughness) {
    float a = roughness * roughness;
    float phi = 2.0 * PI * Xi.x;
    float cosTheta = sqrt((1.0 - Xi.y) / (1.0 + (a * a - 1.0) * Xi.y));
    float sinTheta = sqrt(1.0 - cosTheta * cosTheta);
    vec3 H = vec3(cos(phi) * sinTheta, sin(phi) * sinTheta, cosTheta);
    vec3 up = abs(N.z) < 0.999 ? vec3(0, 0, 1) : vec3(1, 0, 0);
    vec3 tangent = normalize(cross(up, N));
    vec3 bitangent = cross(N, tangent);
    return normalize(tangent * H.x + bitangent * H.y + N * H.z);
}

// IBL geometry: k = roughness^2 / 2  (different from direct lighting)
float GeometrySchlickGGX_IBL(float NdotV, float roughness) {
    float k = (roughness * roughness) / 2.0;
    return NdotV / (NdotV * (1.0 - k) + k);
}
float GeometrySmith_IBL(vec3 N, vec3 V, vec3 L, float roughness) {
    return GeometrySchlickGGX_IBL(max(dot(N, V), 0.0), roughness)
         * GeometrySchlickGGX_IBL(max(dot(N, L), 0.0), roughness);
}

void main() {
    float NdotV = inUV.x;
    float roughness = inUV.y;

    vec3 V = vec3(sqrt(1.0 - NdotV * NdotV), 0.0, NdotV);
    vec3 N = vec3(0.0, 0.0, 1.0);

    float A = 0.0, B = 0.0;
    const uint SAMPLE_COUNT = 1024u;
    for (uint i = 0u; i < SAMPLE_COUNT; ++i) {
        vec2 Xi = Hammersley(i, SAMPLE_COUNT);
        vec3 H  = ImportanceSampleGGX(Xi, N, roughness);
        vec3 L  = normalize(2.0 * dot(V, H) * H - V);

        float NdotL = max(L.z, 0.0);
        float NdotH = max(H.z, 0.0);
        float VdotH = max(dot(V, H), 0.0);

        if (NdotL > 0.0) {
            float G     = GeometrySmith_IBL(N, V, L, roughness);
            float G_Vis = (G * VdotH) / (NdotH * NdotV + 0.0001);
            float Fc    = pow(1.0 - VdotH, 5.0);
            A += (1.0 - Fc) * G_Vis;
            B += Fc * G_Vis;
        }
    }
    outColor = vec2(A, B) / float(SAMPLE_COUNT);
}
```

- [ ] **Step 7: 셰이더 컴파일**

```bash
cd "C:/Users/rlckd/Desktop/kc/Vulkan-Graphics-Example"
glslc shaders/pbr/equirect.vert   -o shaders/pbr/equirect.vert.spv
glslc shaders/pbr/equirect.frag   -o shaders/pbr/equirect.frag.spv
glslc shaders/pbr/irradiance.frag -o shaders/pbr/irradiance.frag.spv
glslc shaders/pbr/prefilter.frag  -o shaders/pbr/prefilter.frag.spv
glslc shaders/pbr/brdf_lut.vert   -o shaders/pbr/brdf_lut.vert.spv
glslc shaders/pbr/brdf_lut.frag   -o shaders/pbr/brdf_lut.frag.spv
```

Expected: 모든 셰이더가 에러 없이 컴파일됨.

- [ ] **Step 8: 커밋**

```bash
rtk git add shaders/pbr/equirect.vert shaders/pbr/equirect.frag \
            shaders/pbr/irradiance.frag shaders/pbr/prefilter.frag \
            shaders/pbr/brdf_lut.vert shaders/pbr/brdf_lut.frag \
            shaders/pbr/equirect.vert.spv shaders/pbr/equirect.frag.spv \
            shaders/pbr/irradiance.frag.spv shaders/pbr/prefilter.frag.spv \
            shaders/pbr/brdf_lut.vert.spv shaders/pbr/brdf_lut.frag.spv
rtk git commit -m "feat(shaders): add IBL generation shaders (equirect, irradiance, prefilter, brdf_lut)"
```

---

## Task 3: pbr.hpp IBL/skybox 멤버 선언

**Files:**
- Modify: `src/examples/pbr/pbr.hpp`

- [ ] **Step 1: Options에 IBL 관련 필드 추가**

`pbr.hpp`의 `Options` 구조체에서 `float ambientStrength = 0.03f;` 다음에 추가:

```cpp
  bool useIBL = false;
  float iblExposure = 4.5f;
  float iblGamma = 2.2f;
```

- [ ] **Step 2: UniformDataComposition에 useIBL 추가**

`pbr.hpp`의 `UniformDataComposition`에서 `float _pad2;` 다음에 추가:

```cpp
  int useIBL{0};
  int _pad3[3];
```

- [ ] **Step 3: CaptureUbo 구조체 추가**

`pbr.hpp`에서 `struct SpritePushConstants` 선언 위에 추가:

```cpp
struct CaptureUbo {
  glm::mat4 mvp;
};

struct SkyboxPushConstants {
  glm::mat4 view;        // translation 제거한 view
  glm::mat4 projection;  // 총 128바이트
};
```

- [ ] **Step 4: VgeExample 클래스에 IBL 멤버 추가**

`pbr.hpp`의 `VgeExample` 클래스에서 `float lightAnimTime = 0.f;` 다음에 추가:

```cpp
  // IBL 텍스처 (startup 1회 생성)
  std::unique_ptr<vgeu::VgeuImage> hdrTexture;
  std::unique_ptr<vgeu::VgeuImage> envCubemap;
  std::unique_ptr<vgeu::VgeuImage> irradianceMap;
  std::unique_ptr<vgeu::VgeuImage> prefilteredMap;
  std::unique_ptr<vgeu::VgeuImage> brdfLut;
  vk::raii::Sampler iblSampler = nullptr;

  // IBL descriptor (composition set=1)
  vk::raii::DescriptorSetLayout iblDescriptorSetLayout = nullptr;
  std::vector<vk::raii::DescriptorSet> iblDescriptorSets;

  // Skybox
  vk::raii::Pipeline skyboxPipeline = nullptr;
  vk::raii::PipelineLayout skyboxPipelineLayout = nullptr;
  vk::raii::DescriptorSetLayout skyboxDescriptorSetLayout = nullptr;
  std::vector<vk::raii::DescriptorSet> skyboxDescriptorSets;
  std::unique_ptr<vgeu::VgeuBuffer> skyboxUboBuffer;  // per-frame, view+proj

  // IBL 생성 함수
  void prepareIBL();
  void loadHdrTexture();
  void buildEnvCubemap();
  void buildIrradianceMap();
  void buildPrefilteredMap();
  void buildBrdfLut();
  void prepareSkyboxPipeline();
  void prepareSkyboxDescriptors();
```

- [ ] **Step 5: clang-format 적용**

```bash
clang-format -i src/examples/pbr/pbr.hpp
```

- [ ] **Step 6: 빌드 확인 (컴파일 에러 없음)**

```bash
cmake --build ./build --config Debug 2>&1 | tail -30
```

- [ ] **Step 7: 커밋**

```bash
rtk git add src/examples/pbr/pbr.hpp
rtk git commit -m "feat(pbr): add IBL/skybox member declarations to pbr.hpp"
```

---

## Task 4: loadHdrTexture + buildEnvCubemap 구현

**Files:**
- Modify: `src/examples/pbr/pbr.cpp`

- [ ] **Step 1: stb_image.h HDR 지원 include 확인**

`pbr.cpp` 상단 includes에 추가 (아직 없다면):

```cpp
#define STB_IMAGE_IMPLEMENTATION
#include <stb_image.h>
```

주의: tinygltf가 이미 STB_IMAGE_IMPLEMENTATION을 정의하므로 충돌 가능. 대신 다음을 확인:
- `src/base/vgeu_texture.cpp`에 이미 `#include <stb_image.h>` 있음
- `pbr.cpp`에서는 정의 없이 `#include <stb_image.h>` 만 추가

- [ ] **Step 2: loadHdrTexture() 구현 추가**

`pbr.cpp`의 `VgeExample::loadAssets()` 함수 바로 뒤에 추가:

```cpp
void VgeExample::loadHdrTexture() {
  std::string path =
      getAssetsPath() + "/textures/hdr/tree_lined_driveway_4k.hdr";
  int w, h, c;
  // force 4 channels (RGBA float)
  float* pixels = stbi_loadf(path.c_str(), &w, &h, &c, 4);
  assert(pixels && "Failed to load HDR file");

  vk::DeviceSize size = static_cast<vk::DeviceSize>(w) * h * 4 * sizeof(float);

  vgeu::VgeuBuffer staging(globalAllocator->getAllocator(), size, 1,
                            vk::BufferUsageFlagBits::eTransferSrc,
                            VMA_MEMORY_USAGE_AUTO,
                            VMA_ALLOCATION_CREATE_HOST_ACCESS_SEQUENTIAL_WRITE_BIT |
                                VMA_ALLOCATION_CREATE_MAPPED_BIT);
  std::memcpy(staging.getMappedData(), pixels, size);
  stbi_image_free(pixels);

  hdrTexture = std::make_unique<vgeu::VgeuImage>(
      device, globalAllocator->getAllocator(),
      vk::Format::eR32G32B32A32Sfloat, vk::Extent2D{(uint32_t)w, (uint32_t)h},
      vk::ImageTiling::eOptimal,
      vk::ImageUsageFlagBits::eSampled | vk::ImageUsageFlagBits::eTransferDst,
      vk::ImageLayout::eUndefined, VmaMemoryUsage::VMA_MEMORY_USAGE_AUTO,
      VmaAllocationCreateFlagBits::VMA_ALLOCATION_CREATE_DEDICATED_MEMORY_BIT,
      vk::ImageAspectFlagBits::eColor, 1);

  vk::BufferImageCopy region(
      0, 0, 0,
      vk::ImageSubresourceLayers{vk::ImageAspectFlagBits::eColor, 0, 0, 1},
      vk::Offset3D{0, 0, 0}, vk::Extent3D{(uint32_t)w, (uint32_t)h, 1});

  vgeu::oneTimeSubmit(
      device, commandPool, queue, [&](const vk::raii::CommandBuffer& cmd) {
        vgeu::setImageLayout(cmd, hdrTexture->getImage(),
                             vk::Format::eR32G32B32A32Sfloat, 0, 1,
                             vk::ImageLayout::eUndefined,
                             vk::ImageLayout::eTransferDstOptimal);
        cmd.copyBufferToImage(staging.getBuffer(), hdrTexture->getImage(),
                              vk::ImageLayout::eTransferDstOptimal, region);
        vgeu::setImageLayout(cmd, hdrTexture->getImage(),
                             vk::Format::eR32G32B32A32Sfloat, 0, 1,
                             vk::ImageLayout::eTransferDstOptimal,
                             vk::ImageLayout::eShaderReadOnlyOptimal);
      });
}
```

- [ ] **Step 3: buildEnvCubemap() 구현 추가**

`loadHdrTexture()` 다음에 추가. 이 함수는 패턴이 길어서 단계별로 작성:

```cpp
void VgeExample::buildEnvCubemap() {
  const vk::Format fmt = vk::Format::eR16G16B16A16Sfloat;
  const uint32_t dim = 512;
  const uint32_t numMips =
      static_cast<uint32_t>(std::floor(std::log2(dim))) + 1;

  // ── 1. 최종 envCubemap 이미지 (SAMPLED | TRANSFER_DST) ──
  envCubemap = std::make_unique<vgeu::VgeuImage>(
      device, globalAllocator->getAllocator(), fmt,
      vk::Extent2D{dim, dim}, vk::ImageTiling::eOptimal,
      vk::ImageUsageFlagBits::eSampled |
          vk::ImageUsageFlagBits::eTransferDst,
      vk::ImageLayout::eUndefined, VmaMemoryUsage::VMA_MEMORY_USAGE_AUTO,
      VmaAllocationCreateFlagBits::VMA_ALLOCATION_CREATE_DEDICATED_MEMORY_BIT,
      vk::ImageAspectFlagBits::eColor, numMips,
      true /*isCubemap*/);

  // ── 2. 임시 offscreen 2D 이미지 (COLOR_ATTACHMENT | TRANSFER_SRC) ──
  auto offscreenImg = std::make_unique<vgeu::VgeuImage>(
      device, globalAllocator->getAllocator(), fmt,
      vk::Extent2D{dim, dim}, vk::ImageTiling::eOptimal,
      vk::ImageUsageFlagBits::eColorAttachment |
          vk::ImageUsageFlagBits::eTransferSrc,
      vk::ImageLayout::eUndefined, VmaMemoryUsage::VMA_MEMORY_USAGE_AUTO,
      VmaAllocationCreateFlagBits::VMA_ALLOCATION_CREATE_DEDICATED_MEMORY_BIT,
      vk::ImageAspectFlagBits::eColor, 1);

  // ── 3. CaptureUbo buffer ──
  auto captureUboBuf = std::make_unique<vgeu::VgeuBuffer>(
      globalAllocator->getAllocator(), sizeof(CaptureUbo), 1,
      vk::BufferUsageFlagBits::eUniformBuffer, VMA_MEMORY_USAGE_AUTO,
      VMA_ALLOCATION_CREATE_HOST_ACCESS_SEQUENTIAL_WRITE_BIT |
          VMA_ALLOCATION_CREATE_MAPPED_BIT);

  // ── 4. RenderPass (single color attachment) ──
  vk::AttachmentDescription attDesc(
      {}, fmt, vk::SampleCountFlagBits::e1, vk::AttachmentLoadOp::eClear,
      vk::AttachmentStoreOp::eStore, vk::AttachmentLoadOp::eDontCare,
      vk::AttachmentStoreOp::eDontCare, vk::ImageLayout::eUndefined,
      vk::ImageLayout::eColorAttachmentOptimal);
  vk::AttachmentReference colorRef(0, vk::ImageLayout::eColorAttachmentOptimal);
  vk::SubpassDescription subpass({}, vk::PipelineBindPoint::eGraphics, {},
                                 colorRef);
  std::array<vk::SubpassDependency, 2> deps{
      vk::SubpassDependency(VK_SUBPASS_EXTERNAL, 0,
          vk::PipelineStageFlagBits::eBottomOfPipe,
          vk::PipelineStageFlagBits::eColorAttachmentOutput,
          vk::AccessFlagBits::eMemoryRead,
          vk::AccessFlagBits::eColorAttachmentWrite,
          vk::DependencyFlagBits::eByRegion),
      vk::SubpassDependency(0, VK_SUBPASS_EXTERNAL,
          vk::PipelineStageFlagBits::eColorAttachmentOutput,
          vk::PipelineStageFlagBits::eBottomOfPipe,
          vk::AccessFlagBits::eColorAttachmentWrite,
          vk::AccessFlagBits::eMemoryRead,
          vk::DependencyFlagBits::eByRegion)};
  auto captureRenderPass = vk::raii::RenderPass(
      device, vk::RenderPassCreateInfo({}, attDesc, subpass, deps));

  // ── 5. Framebuffer ──
  vk::ImageView offscreenView = *offscreenImg->getImageView();
  auto captureFBO = vk::raii::Framebuffer(
      device, vk::FramebufferCreateInfo({}, *captureRenderPass,
                                        offscreenView, dim, dim, 1));

  // ── 6. Descriptor set layouts ──
  vk::DescriptorSetLayoutBinding uboBinding(
      0, vk::DescriptorType::eUniformBuffer, 1,
      vk::ShaderStageFlagBits::eVertex);
  auto captureDSL = vk::raii::DescriptorSetLayout(
      device, vk::DescriptorSetLayoutCreateInfo({}, uboBinding));

  vk::DescriptorSetLayoutBinding hdrBinding(
      0, vk::DescriptorType::eCombinedImageSampler, 1,
      vk::ShaderStageFlagBits::eFragment);
  auto hdrDSL = vk::raii::DescriptorSetLayout(
      device, vk::DescriptorSetLayoutCreateInfo({}, hdrBinding));

  // ── 7. Descriptor pool + sets ──
  std::vector<vk::DescriptorPoolSize> poolSizes{
      {vk::DescriptorType::eUniformBuffer, 1},
      {vk::DescriptorType::eCombinedImageSampler, 1}};
  auto capturePool = vk::raii::DescriptorPool(
      device, vk::DescriptorPoolCreateInfo(
                  vk::DescriptorPoolCreateFlagBits::eFreeDescriptorSet,
                  2, poolSizes));

  auto captureUboDS =
      std::move(vk::raii::DescriptorSets(
                    device, vk::DescriptorSetAllocateInfo(*capturePool, *captureDSL))
                    .front());
  auto hdrDS =
      std::move(vk::raii::DescriptorSets(
                    device, vk::DescriptorSetAllocateInfo(*capturePool, *hdrDSL))
                    .front());

  // Write UBO descriptor
  auto uboBufInfo = captureUboBuf->descriptorInfo();
  device.updateDescriptorSets(
      vk::WriteDescriptorSet(*captureUboDS, 0, 0,
                             vk::DescriptorType::eUniformBuffer, nullptr,
                             uboBufInfo),
      nullptr);
  // Write HDR sampler descriptor (use iblSampler)
  auto hdrImgInfo = hdrTexture->descriptorImageInfo(
      *iblSampler, vk::ImageLayout::eShaderReadOnlyOptimal);
  device.updateDescriptorSets(
      vk::WriteDescriptorSet(*hdrDS, 0, 0,
                             vk::DescriptorType::eCombinedImageSampler,
                             hdrImgInfo, nullptr),
      nullptr);

  // ── 8. Pipeline layout ──
  std::vector<vk::DescriptorSetLayout> setLayouts{*captureDSL, *hdrDSL};
  auto capturePipelineLayout = vk::raii::PipelineLayout(
      device, vk::PipelineLayoutCreateInfo({}, setLayouts));

  // ── 9. Pipeline (equirect.vert + equirect.frag, cull=NONE, no vertex input)
  auto vertCode =
      vgeu::readFile(getShadersPath() + "/pbr/equirect.vert.spv");
  auto fragCode =
      vgeu::readFile(getShadersPath() + "/pbr/equirect.frag.spv");
  auto vertMod = vgeu::createShaderModule(device, vertCode);
  auto fragMod = vgeu::createShaderModule(device, fragCode);

  std::array<vk::PipelineShaderStageCreateInfo, 2> stages{
      vk::PipelineShaderStageCreateInfo({}, vk::ShaderStageFlagBits::eVertex,
                                        *vertMod, "main"),
      vk::PipelineShaderStageCreateInfo({}, vk::ShaderStageFlagBits::eFragment,
                                        *fragMod, "main")};
  vk::PipelineVertexInputStateCreateInfo emptyVI{};
  vk::PipelineInputAssemblyStateCreateInfo iaCI(
      {}, vk::PrimitiveTopology::eTriangleList);
  vk::PipelineRasterizationStateCreateInfo rasCI(
      {}, false, false, vk::PolygonMode::eFill, vk::CullModeFlagBits::eNone,
      vk::FrontFace::eCounterClockwise, false, 0, 0, 0, 1.f);
  vk::PipelineColorBlendAttachmentState blendAtt(
      false, {}, {}, {}, {}, {}, {},
      vk::ColorComponentFlagBits::eR | vk::ColorComponentFlagBits::eG |
          vk::ColorComponentFlagBits::eB | vk::ColorComponentFlagBits::eA);
  vk::PipelineColorBlendStateCreateInfo cbCI({}, false, {}, blendAtt);
  vk::PipelineDepthStencilStateCreateInfo dsCI(
      {}, false, false, vk::CompareOp::eLessOrEqual);
  vk::PipelineViewportStateCreateInfo vpCI({}, 1, nullptr, 1, nullptr);
  vk::PipelineMultisampleStateCreateInfo msCI({}, vk::SampleCountFlagBits::e1);
  std::array<vk::DynamicState, 2> dynStates{vk::DynamicState::eViewport,
                                            vk::DynamicState::eScissor};
  vk::PipelineDynamicStateCreateInfo dynCI({}, dynStates);

  auto capturePipeline = vk::raii::Pipeline(
      device, pipelineCache,
      vk::GraphicsPipelineCreateInfo({}, stages, &emptyVI, &iaCI, nullptr,
                                     &vpCI, &rasCI, &msCI, &dsCI, &cbCI,
                                     &dynCI, *capturePipelineLayout,
                                     *captureRenderPass));

  // ── 10. 캡처 rotation matrices (Sascha 방식) ──
  std::vector<glm::mat4> rotMatrices = {
      glm::rotate(glm::rotate(glm::mat4(1.f), glm::radians(90.f),
                              glm::vec3(0, 1, 0)),
                  glm::radians(180.f), glm::vec3(1, 0, 0)),  // +X
      glm::rotate(glm::rotate(glm::mat4(1.f), glm::radians(-90.f),
                              glm::vec3(0, 1, 0)),
                  glm::radians(180.f), glm::vec3(1, 0, 0)),  // -X
      glm::rotate(glm::mat4(1.f), glm::radians(-90.f),
                  glm::vec3(1, 0, 0)),  // +Y
      glm::rotate(glm::mat4(1.f), glm::radians(90.f),
                  glm::vec3(1, 0, 0)),  // -Y
      glm::rotate(glm::mat4(1.f), glm::radians(180.f),
                  glm::vec3(1, 0, 0)),  // +Z
      glm::rotate(glm::mat4(1.f), glm::radians(180.f),
                  glm::vec3(0, 0, 1)),  // -Z
  };
  glm::mat4 captureProj =
      glm::perspective(glm::radians(90.f), 1.f, 0.1f, 512.f);

  // ── 11. oneTimeSubmit: 6면 캡처 + copy ──
  vgeu::oneTimeSubmit(
      device, commandPool, queue, [&](const vk::raii::CommandBuffer& cmd) {
        // envCubemap 전체를 TRANSFER_DST로 전환
        vgeu::setImageLayout(
            cmd, envCubemap->getImage(), fmt,
            vk::ImageSubresourceRange{vk::ImageAspectFlagBits::eColor, 0,
                                      numMips, 0, 6},
            vk::ImageLayout::eUndefined,
            vk::ImageLayout::eTransferDstOptimal);
        // offscreen을 COLOR_ATTACHMENT로 전환
        vgeu::setImageLayout(cmd, offscreenImg->getImage(), fmt, 0, 1,
                             vk::ImageLayout::eUndefined,
                             vk::ImageLayout::eColorAttachmentOptimal);

        vk::ClearValue clearVal;
        clearVal.color = vk::ClearColorValue(0.f, 0.f, 0.f, 1.f);

        for (uint32_t f = 0; f < 6; ++f) {
          // MVP 업데이트
          CaptureUbo uboData;
          uboData.mvp = captureProj * rotMatrices[f];
          std::memcpy(captureUboBuf->getMappedData(), &uboData,
                      sizeof(CaptureUbo));

          cmd.beginRenderPass(
              vk::RenderPassBeginInfo(*captureRenderPass, *captureFBO,
                                      vk::Rect2D({}, vk::Extent2D{dim, dim}),
                                      clearVal),
              vk::SubpassContents::eInline);
          cmd.setViewport(
              0, vk::Viewport(0.f, 0.f, (float)dim, (float)dim, 0.f, 1.f));
          cmd.setScissor(0, vk::Rect2D({}, vk::Extent2D{dim, dim}));
          cmd.bindPipeline(vk::PipelineBindPoint::eGraphics, *capturePipeline);
          cmd.bindDescriptorSets(vk::PipelineBindPoint::eGraphics,
                                 *capturePipelineLayout, 0,
                                 {*captureUboDS, *hdrDS}, nullptr);
          cmd.draw(36, 1, 0, 0);
          cmd.endRenderPass();

          // offscreen → TRANSFER_SRC
          vgeu::setImageLayout(cmd, offscreenImg->getImage(), fmt, 0, 1,
                               vk::ImageLayout::eColorAttachmentOptimal,
                               vk::ImageLayout::eTransferSrcOptimal);

          // copy → envCubemap[face=f][mip=0]
          vk::ImageCopy copyRegion(
              vk::ImageSubresourceLayers{vk::ImageAspectFlagBits::eColor, 0, 0,
                                        1},
              vk::Offset3D{0, 0, 0},
              vk::ImageSubresourceLayers{vk::ImageAspectFlagBits::eColor, 0, f,
                                        1},
              vk::Offset3D{0, 0, 0}, vk::Extent3D{dim, dim, 1});
          cmd.copyImage(offscreenImg->getImage(),
                        vk::ImageLayout::eTransferSrcOptimal,
                        envCubemap->getImage(),
                        vk::ImageLayout::eTransferDstOptimal, copyRegion);

          // offscreen → COLOR_ATTACHMENT 복원
          vgeu::setImageLayout(cmd, offscreenImg->getImage(), fmt, 0, 1,
                               vk::ImageLayout::eTransferSrcOptimal,
                               vk::ImageLayout::eColorAttachmentOptimal);
        }

        // mip 0 → SHADER_READ_ONLY (나머지 mip은 generateMipmaps에서 처리)
        vgeu::setImageLayout(
            cmd, envCubemap->getImage(), fmt,
            vk::ImageSubresourceRange{vk::ImageAspectFlagBits::eColor, 0, 1, 0,
                                      6},
            vk::ImageLayout::eTransferDstOptimal,
            vk::ImageLayout::eTransferSrcOptimal);  // blit 준비용
      });

  // ── 12. envCubemap mip 생성 (mip 0 → 1..numMips-1, 각 6 face) ──
  vgeu::oneTimeSubmit(
      device, commandPool, queue, [&](const vk::raii::CommandBuffer& cmd) {
        uint32_t mipWidth = dim, mipHeight = dim;
        for (uint32_t m = 1; m < numMips; ++m) {
          uint32_t nextW = mipWidth > 1 ? mipWidth / 2 : 1u;
          uint32_t nextH = mipHeight > 1 ? mipHeight / 2 : 1u;

          // mip m을 TRANSFER_DST로
          vgeu::setImageLayout(
              cmd, envCubemap->getImage(), fmt,
              vk::ImageSubresourceRange{vk::ImageAspectFlagBits::eColor, m, 1,
                                        0, 6},
              vk::ImageLayout::eTransferDstOptimal,
              vk::ImageLayout::eTransferDstOptimal);

          vk::ImageBlit blit(
              vk::ImageSubresourceLayers{vk::ImageAspectFlagBits::eColor, m - 1,
                                        0, 6},
              {vk::Offset3D{0, 0, 0},
               vk::Offset3D{(int)mipWidth, (int)mipHeight, 1}},
              vk::ImageSubresourceLayers{vk::ImageAspectFlagBits::eColor, m, 0,
                                        6},
              {vk::Offset3D{0, 0, 0},
               vk::Offset3D{(int)nextW, (int)nextH, 1}});
          cmd.blitImage(envCubemap->getImage(),
                        vk::ImageLayout::eTransferSrcOptimal,
                        envCubemap->getImage(),
                        vk::ImageLayout::eTransferDstOptimal, blit,
                        vk::Filter::eLinear);

          // mip m → SHADER_READ_ONLY
          vgeu::setImageLayout(
              cmd, envCubemap->getImage(), fmt,
              vk::ImageSubresourceRange{vk::ImageAspectFlagBits::eColor, m, 1,
                                        0, 6},
              vk::ImageLayout::eTransferDstOptimal,
              vk::ImageLayout::eShaderReadOnlyOptimal);
          // mip m-1 blit 전환 완료 → SHADER_READ_ONLY (첫 번째 후 동작)
          if (m == numMips - 1) {
            vgeu::setImageLayout(
                cmd, envCubemap->getImage(), fmt,
                vk::ImageSubresourceRange{vk::ImageAspectFlagBits::eColor, m,
                                          1, 0, 6},
                vk::ImageLayout::eTransferDstOptimal,
                vk::ImageLayout::eShaderReadOnlyOptimal);
          }
          mipWidth = nextW;
          mipHeight = nextH;
        }
        // mip 0 → SHADER_READ_ONLY
        vgeu::setImageLayout(
            cmd, envCubemap->getImage(), fmt,
            vk::ImageSubresourceRange{vk::ImageAspectFlagBits::eColor, 0, 1, 0,
                                      6},
            vk::ImageLayout::eTransferSrcOptimal,
            vk::ImageLayout::eShaderReadOnlyOptimal);
      });
  // ── 임시 자원은 스코프 종료 시 자동 소멸 ──
}
```

- [ ] **Step 4: clang-format 적용**

```bash
clang-format -i src/examples/pbr/pbr.cpp
```

- [ ] **Step 5: 빌드 확인**

```bash
cmake --build ./build --config Debug 2>&1 | tail -30
```

- [ ] **Step 6: 커밋**

```bash
rtk git add src/examples/pbr/pbr.cpp
rtk git commit -m "feat(pbr): implement loadHdrTexture and buildEnvCubemap"
```

---

## Task 5: buildIrradianceMap + buildPrefilteredMap 구현

**Files:**
- Modify: `src/examples/pbr/pbr.cpp`

> 두 함수 모두 Task 4와 동일한 offscreen 2D → copy 패턴. 차이는 셰이더/해상도/push constant.

- [ ] **Step 1: buildIrradianceMap() 구현**

`buildEnvCubemap()` 다음에 추가. Task 4의 buildEnvCubemap과 구조 동일, 달라지는 부분만 표시:

```cpp
void VgeExample::buildIrradianceMap() {
  const vk::Format fmt = vk::Format::eR32G32B32A32Sfloat;
  const uint32_t dim = 64;
  const uint32_t numMips =
      static_cast<uint32_t>(std::floor(std::log2(dim))) + 1;

  // 최종 irradianceMap
  irradianceMap = std::make_unique<vgeu::VgeuImage>(
      device, globalAllocator->getAllocator(), fmt, vk::Extent2D{dim, dim},
      vk::ImageTiling::eOptimal,
      vk::ImageUsageFlagBits::eSampled | vk::ImageUsageFlagBits::eTransferDst,
      vk::ImageLayout::eUndefined, VmaMemoryUsage::VMA_MEMORY_USAGE_AUTO,
      VmaAllocationCreateFlagBits::VMA_ALLOCATION_CREATE_DEDICATED_MEMORY_BIT,
      vk::ImageAspectFlagBits::eColor, numMips, true);

  // offscreen 2D
  auto offscreenImg = std::make_unique<vgeu::VgeuImage>(
      device, globalAllocator->getAllocator(), fmt, vk::Extent2D{dim, dim},
      vk::ImageTiling::eOptimal,
      vk::ImageUsageFlagBits::eColorAttachment |
          vk::ImageUsageFlagBits::eTransferSrc,
      vk::ImageLayout::eUndefined, VmaMemoryUsage::VMA_MEMORY_USAGE_AUTO,
      VmaAllocationCreateFlagBits::VMA_ALLOCATION_CREATE_DEDICATED_MEMORY_BIT,
      vk::ImageAspectFlagBits::eColor, 1);

  // CaptureUbo buffer
  auto captureUboBuf = std::make_unique<vgeu::VgeuBuffer>(
      globalAllocator->getAllocator(), sizeof(CaptureUbo), 1,
      vk::BufferUsageFlagBits::eUniformBuffer, VMA_MEMORY_USAGE_AUTO,
      VMA_ALLOCATION_CREATE_HOST_ACCESS_SEQUENTIAL_WRITE_BIT |
          VMA_ALLOCATION_CREATE_MAPPED_BIT);

  // RenderPass (buildEnvCubemap와 동일 구조, fmt만 다름)
  vk::AttachmentDescription attDesc(
      {}, fmt, vk::SampleCountFlagBits::e1, vk::AttachmentLoadOp::eClear,
      vk::AttachmentStoreOp::eStore, vk::AttachmentLoadOp::eDontCare,
      vk::AttachmentStoreOp::eDontCare, vk::ImageLayout::eUndefined,
      vk::ImageLayout::eColorAttachmentOptimal);
  vk::AttachmentReference colorRef(0, vk::ImageLayout::eColorAttachmentOptimal);
  vk::SubpassDescription subpass({}, vk::PipelineBindPoint::eGraphics, {},
                                 colorRef);
  std::array<vk::SubpassDependency, 2> deps{
      vk::SubpassDependency(VK_SUBPASS_EXTERNAL, 0,
          vk::PipelineStageFlagBits::eBottomOfPipe,
          vk::PipelineStageFlagBits::eColorAttachmentOutput,
          vk::AccessFlagBits::eMemoryRead,
          vk::AccessFlagBits::eColorAttachmentWrite,
          vk::DependencyFlagBits::eByRegion),
      vk::SubpassDependency(0, VK_SUBPASS_EXTERNAL,
          vk::PipelineStageFlagBits::eColorAttachmentOutput,
          vk::PipelineStageFlagBits::eBottomOfPipe,
          vk::AccessFlagBits::eColorAttachmentWrite,
          vk::AccessFlagBits::eMemoryRead,
          vk::DependencyFlagBits::eByRegion)};
  auto captureRenderPass = vk::raii::RenderPass(
      device, vk::RenderPassCreateInfo({}, attDesc, subpass, deps));
  vk::ImageView offscreenView = *offscreenImg->getImageView();
  auto captureFBO = vk::raii::Framebuffer(
      device, vk::FramebufferCreateInfo({}, *captureRenderPass,
                                        offscreenView, dim, dim, 1));

  // Descriptor layouts: set=0 UBO, set=1 samplerCube
  vk::DescriptorSetLayoutBinding uboBinding(
      0, vk::DescriptorType::eUniformBuffer, 1,
      vk::ShaderStageFlagBits::eVertex);
  auto captureDSL = vk::raii::DescriptorSetLayout(
      device, vk::DescriptorSetLayoutCreateInfo({}, uboBinding));
  vk::DescriptorSetLayoutBinding envBinding(
      0, vk::DescriptorType::eCombinedImageSampler, 1,
      vk::ShaderStageFlagBits::eFragment);
  auto envDSL = vk::raii::DescriptorSetLayout(
      device, vk::DescriptorSetLayoutCreateInfo({}, envBinding));

  std::vector<vk::DescriptorPoolSize> poolSizes{
      {vk::DescriptorType::eUniformBuffer, 1},
      {vk::DescriptorType::eCombinedImageSampler, 1}};
  auto capturePool = vk::raii::DescriptorPool(
      device, vk::DescriptorPoolCreateInfo(
                  vk::DescriptorPoolCreateFlagBits::eFreeDescriptorSet,
                  2, poolSizes));
  auto captureUboDS =
      std::move(vk::raii::DescriptorSets(
                    device, vk::DescriptorSetAllocateInfo(*capturePool,
                                                          *captureDSL))
                    .front());
  auto envDS =
      std::move(vk::raii::DescriptorSets(
                    device,
                    vk::DescriptorSetAllocateInfo(*capturePool, *envDSL))
                    .front());

  auto uboBufInfo = captureUboBuf->descriptorInfo();
  device.updateDescriptorSets(
      vk::WriteDescriptorSet(*captureUboDS, 0, 0,
                             vk::DescriptorType::eUniformBuffer, nullptr,
                             uboBufInfo),
      nullptr);
  auto envImgInfo = envCubemap->descriptorImageInfo(
      *iblSampler, vk::ImageLayout::eShaderReadOnlyOptimal);
  device.updateDescriptorSets(
      vk::WriteDescriptorSet(*envDS, 0, 0,
                             vk::DescriptorType::eCombinedImageSampler,
                             envImgInfo, nullptr),
      nullptr);

  // Push constant range: deltaPhi + deltaTheta = 8 bytes
  vk::PushConstantRange pcRange(
      vk::ShaderStageFlagBits::eFragment, 0, 8u);
  std::vector<vk::DescriptorSetLayout> setLayouts{*captureDSL, *envDSL};
  auto capturePipelineLayout = vk::raii::PipelineLayout(
      device, vk::PipelineLayoutCreateInfo({}, setLayouts, pcRange));

  // Pipeline (equirect.vert + irradiance.frag)
  auto vertCode =
      vgeu::readFile(getShadersPath() + "/pbr/equirect.vert.spv");
  auto fragCode =
      vgeu::readFile(getShadersPath() + "/pbr/irradiance.frag.spv");
  auto vertMod = vgeu::createShaderModule(device, vertCode);
  auto fragMod = vgeu::createShaderModule(device, fragCode);
  std::array<vk::PipelineShaderStageCreateInfo, 2> stages{
      vk::PipelineShaderStageCreateInfo({}, vk::ShaderStageFlagBits::eVertex,
                                        *vertMod, "main"),
      vk::PipelineShaderStageCreateInfo({}, vk::ShaderStageFlagBits::eFragment,
                                        *fragMod, "main")};
  vk::PipelineVertexInputStateCreateInfo emptyVI{};
  vk::PipelineInputAssemblyStateCreateInfo iaCI(
      {}, vk::PrimitiveTopology::eTriangleList);
  vk::PipelineRasterizationStateCreateInfo rasCI(
      {}, false, false, vk::PolygonMode::eFill, vk::CullModeFlagBits::eNone,
      vk::FrontFace::eCounterClockwise, false, 0, 0, 0, 1.f);
  vk::PipelineColorBlendAttachmentState blendAtt(
      false, {}, {}, {}, {}, {}, {},
      vk::ColorComponentFlagBits::eR | vk::ColorComponentFlagBits::eG |
          vk::ColorComponentFlagBits::eB | vk::ColorComponentFlagBits::eA);
  vk::PipelineColorBlendStateCreateInfo cbCI({}, false, {}, blendAtt);
  vk::PipelineDepthStencilStateCreateInfo dsCI(
      {}, false, false, vk::CompareOp::eLessOrEqual);
  vk::PipelineViewportStateCreateInfo vpCI({}, 1, nullptr, 1, nullptr);
  vk::PipelineMultisampleStateCreateInfo msCI({}, vk::SampleCountFlagBits::e1);
  std::array<vk::DynamicState, 2> dynStates{vk::DynamicState::eViewport,
                                            vk::DynamicState::eScissor};
  vk::PipelineDynamicStateCreateInfo dynCI({}, dynStates);
  auto capturePipeline = vk::raii::Pipeline(
      device, pipelineCache,
      vk::GraphicsPipelineCreateInfo({}, stages, &emptyVI, &iaCI, nullptr,
                                     &vpCI, &rasCI, &msCI, &dsCI, &cbCI,
                                     &dynCI, *capturePipelineLayout,
                                     *captureRenderPass));

  // 6면 회전 행렬 (buildEnvCubemap와 동일)
  std::vector<glm::mat4> rotMatrices = {
      glm::rotate(glm::rotate(glm::mat4(1.f), glm::radians(90.f),
                              glm::vec3(0,1,0)), glm::radians(180.f), glm::vec3(1,0,0)),
      glm::rotate(glm::rotate(glm::mat4(1.f), glm::radians(-90.f),
                              glm::vec3(0,1,0)), glm::radians(180.f), glm::vec3(1,0,0)),
      glm::rotate(glm::mat4(1.f), glm::radians(-90.f), glm::vec3(1,0,0)),
      glm::rotate(glm::mat4(1.f), glm::radians(90.f), glm::vec3(1,0,0)),
      glm::rotate(glm::mat4(1.f), glm::radians(180.f), glm::vec3(1,0,0)),
      glm::rotate(glm::mat4(1.f), glm::radians(180.f), glm::vec3(0,0,1)),
  };
  glm::mat4 captureProj =
      glm::perspective(glm::radians(90.f), 1.f, 0.1f, 512.f);

  struct IrradiancePush { float deltaPhi; float deltaTheta; };

  vgeu::oneTimeSubmit(
      device, commandPool, queue, [&](const vk::raii::CommandBuffer& cmd) {
        vgeu::setImageLayout(
            cmd, irradianceMap->getImage(), fmt,
            vk::ImageSubresourceRange{vk::ImageAspectFlagBits::eColor, 0,
                                      numMips, 0, 6},
            vk::ImageLayout::eUndefined,
            vk::ImageLayout::eTransferDstOptimal);
        vgeu::setImageLayout(cmd, offscreenImg->getImage(), fmt, 0, 1,
                             vk::ImageLayout::eUndefined,
                             vk::ImageLayout::eColorAttachmentOptimal);

        IrradiancePush push{(2.f * glm::pi<float>()) / 180.f,
                            (0.5f * glm::pi<float>()) / 64.f};
        vk::ClearValue clearVal;
        clearVal.color = vk::ClearColorValue(0.f, 0.f, 0.f, 1.f);

        for (uint32_t f = 0; f < 6; ++f) {
          CaptureUbo uboData;
          uboData.mvp = captureProj * rotMatrices[f];
          std::memcpy(captureUboBuf->getMappedData(), &uboData,
                      sizeof(CaptureUbo));

          cmd.beginRenderPass(
              vk::RenderPassBeginInfo(*captureRenderPass, *captureFBO,
                                      vk::Rect2D({}, vk::Extent2D{dim, dim}),
                                      clearVal),
              vk::SubpassContents::eInline);
          cmd.setViewport(
              0, vk::Viewport(0, 0, (float)dim, (float)dim, 0.f, 1.f));
          cmd.setScissor(0, vk::Rect2D({}, vk::Extent2D{dim, dim}));
          cmd.bindPipeline(vk::PipelineBindPoint::eGraphics, *capturePipeline);
          cmd.bindDescriptorSets(vk::PipelineBindPoint::eGraphics,
                                 *capturePipelineLayout, 0,
                                 {*captureUboDS, *envDS}, nullptr);
          cmd.pushConstants<IrradiancePush>(*capturePipelineLayout,
                                            vk::ShaderStageFlagBits::eFragment,
                                            0, push);
          cmd.draw(36, 1, 0, 0);
          cmd.endRenderPass();

          vgeu::setImageLayout(cmd, offscreenImg->getImage(), fmt, 0, 1,
                               vk::ImageLayout::eColorAttachmentOptimal,
                               vk::ImageLayout::eTransferSrcOptimal);
          vk::ImageCopy copyRegion(
              vk::ImageSubresourceLayers{vk::ImageAspectFlagBits::eColor, 0, 0, 1},
              {}, vk::ImageSubresourceLayers{vk::ImageAspectFlagBits::eColor, 0, f, 1},
              {}, vk::Extent3D{dim, dim, 1});
          cmd.copyImage(offscreenImg->getImage(),
                        vk::ImageLayout::eTransferSrcOptimal,
                        irradianceMap->getImage(),
                        vk::ImageLayout::eTransferDstOptimal, copyRegion);
          vgeu::setImageLayout(cmd, offscreenImg->getImage(), fmt, 0, 1,
                               vk::ImageLayout::eTransferSrcOptimal,
                               vk::ImageLayout::eColorAttachmentOptimal);
        }

        vgeu::setImageLayout(
            cmd, irradianceMap->getImage(), fmt,
            vk::ImageSubresourceRange{vk::ImageAspectFlagBits::eColor, 0,
                                      numMips, 0, 6},
            vk::ImageLayout::eTransferDstOptimal,
            vk::ImageLayout::eShaderReadOnlyOptimal);
      });
}
```

- [ ] **Step 2: buildPrefilteredMap() 구현**

`buildIrradianceMap()` 다음에 추가. 외부 루프는 mip, 내부 루프는 face:

```cpp
void VgeExample::buildPrefilteredMap() {
  const vk::Format fmt = vk::Format::eR16G16B16A16Sfloat;
  const uint32_t dim = 512;
  const uint32_t numMips =
      static_cast<uint32_t>(std::floor(std::log2(dim))) + 1;

  prefilteredMap = std::make_unique<vgeu::VgeuImage>(
      device, globalAllocator->getAllocator(), fmt, vk::Extent2D{dim, dim},
      vk::ImageTiling::eOptimal,
      vk::ImageUsageFlagBits::eSampled | vk::ImageUsageFlagBits::eTransferDst,
      vk::ImageLayout::eUndefined, VmaMemoryUsage::VMA_MEMORY_USAGE_AUTO,
      VmaAllocationCreateFlagBits::VMA_ALLOCATION_CREATE_DEDICATED_MEMORY_BIT,
      vk::ImageAspectFlagBits::eColor, numMips, true);

  auto offscreenImg = std::make_unique<vgeu::VgeuImage>(
      device, globalAllocator->getAllocator(), fmt, vk::Extent2D{dim, dim},
      vk::ImageTiling::eOptimal,
      vk::ImageUsageFlagBits::eColorAttachment |
          vk::ImageUsageFlagBits::eTransferSrc,
      vk::ImageLayout::eUndefined, VmaMemoryUsage::VMA_MEMORY_USAGE_AUTO,
      VmaAllocationCreateFlagBits::VMA_ALLOCATION_CREATE_DEDICATED_MEMORY_BIT,
      vk::ImageAspectFlagBits::eColor, 1);

  auto captureUboBuf = std::make_unique<vgeu::VgeuBuffer>(
      globalAllocator->getAllocator(), sizeof(CaptureUbo), 1,
      vk::BufferUsageFlagBits::eUniformBuffer, VMA_MEMORY_USAGE_AUTO,
      VMA_ALLOCATION_CREATE_HOST_ACCESS_SEQUENTIAL_WRITE_BIT |
          VMA_ALLOCATION_CREATE_MAPPED_BIT);

  // RenderPass
  vk::AttachmentDescription attDesc(
      {}, fmt, vk::SampleCountFlagBits::e1, vk::AttachmentLoadOp::eClear,
      vk::AttachmentStoreOp::eStore, vk::AttachmentLoadOp::eDontCare,
      vk::AttachmentStoreOp::eDontCare, vk::ImageLayout::eUndefined,
      vk::ImageLayout::eColorAttachmentOptimal);
  vk::AttachmentReference colorRef(0, vk::ImageLayout::eColorAttachmentOptimal);
  vk::SubpassDescription subpass({}, vk::PipelineBindPoint::eGraphics, {},
                                 colorRef);
  std::array<vk::SubpassDependency, 2> deps{
      vk::SubpassDependency(VK_SUBPASS_EXTERNAL, 0,
          vk::PipelineStageFlagBits::eBottomOfPipe,
          vk::PipelineStageFlagBits::eColorAttachmentOutput,
          vk::AccessFlagBits::eMemoryRead,
          vk::AccessFlagBits::eColorAttachmentWrite,
          vk::DependencyFlagBits::eByRegion),
      vk::SubpassDependency(0, VK_SUBPASS_EXTERNAL,
          vk::PipelineStageFlagBits::eColorAttachmentOutput,
          vk::PipelineStageFlagBits::eBottomOfPipe,
          vk::AccessFlagBits::eColorAttachmentWrite,
          vk::AccessFlagBits::eMemoryRead,
          vk::DependencyFlagBits::eByRegion)};
  auto captureRenderPass = vk::raii::RenderPass(
      device, vk::RenderPassCreateInfo({}, attDesc, subpass, deps));
  vk::ImageView offscreenView = *offscreenImg->getImageView();
  auto captureFBO = vk::raii::Framebuffer(
      device, vk::FramebufferCreateInfo({}, *captureRenderPass,
                                        offscreenView, dim, dim, 1));

  // Descriptor layouts
  vk::DescriptorSetLayoutBinding uboBinding(
      0, vk::DescriptorType::eUniformBuffer, 1,
      vk::ShaderStageFlagBits::eVertex);
  auto captureDSL = vk::raii::DescriptorSetLayout(
      device, vk::DescriptorSetLayoutCreateInfo({}, uboBinding));
  vk::DescriptorSetLayoutBinding envBinding(
      0, vk::DescriptorType::eCombinedImageSampler, 1,
      vk::ShaderStageFlagBits::eFragment);
  auto envDSL = vk::raii::DescriptorSetLayout(
      device, vk::DescriptorSetLayoutCreateInfo({}, envBinding));

  std::vector<vk::DescriptorPoolSize> poolSizes{
      {vk::DescriptorType::eUniformBuffer, 1},
      {vk::DescriptorType::eCombinedImageSampler, 1}};
  auto capturePool = vk::raii::DescriptorPool(
      device, vk::DescriptorPoolCreateInfo(
                  vk::DescriptorPoolCreateFlagBits::eFreeDescriptorSet, 2,
                  poolSizes));
  auto captureUboDS =
      std::move(vk::raii::DescriptorSets(
                    device, vk::DescriptorSetAllocateInfo(*capturePool,
                                                          *captureDSL))
                    .front());
  auto envDS =
      std::move(vk::raii::DescriptorSets(
                    device,
                    vk::DescriptorSetAllocateInfo(*capturePool, *envDSL))
                    .front());

  auto uboBufInfo = captureUboBuf->descriptorInfo();
  device.updateDescriptorSets(
      vk::WriteDescriptorSet(*captureUboDS, 0, 0,
                             vk::DescriptorType::eUniformBuffer, nullptr,
                             uboBufInfo),
      nullptr);
  auto envImgInfo = envCubemap->descriptorImageInfo(
      *iblSampler, vk::ImageLayout::eShaderReadOnlyOptimal);
  device.updateDescriptorSets(
      vk::WriteDescriptorSet(*envDS, 0, 0,
                             vk::DescriptorType::eCombinedImageSampler,
                             envImgInfo, nullptr),
      nullptr);

  // Push constant: roughness(float) + numSamples(uint) = 8 bytes
  vk::PushConstantRange pcRange(
      vk::ShaderStageFlagBits::eFragment, 0, 8u);
  std::vector<vk::DescriptorSetLayout> setLayouts{*captureDSL, *envDSL};
  auto capturePipelineLayout = vk::raii::PipelineLayout(
      device, vk::PipelineLayoutCreateInfo({}, setLayouts, pcRange));

  // Pipeline (equirect.vert + prefilter.frag)
  auto vertCode =
      vgeu::readFile(getShadersPath() + "/pbr/equirect.vert.spv");
  auto fragCode =
      vgeu::readFile(getShadersPath() + "/pbr/prefilter.frag.spv");
  auto vertMod = vgeu::createShaderModule(device, vertCode);
  auto fragMod = vgeu::createShaderModule(device, fragCode);
  std::array<vk::PipelineShaderStageCreateInfo, 2> stages{
      vk::PipelineShaderStageCreateInfo({}, vk::ShaderStageFlagBits::eVertex,
                                        *vertMod, "main"),
      vk::PipelineShaderStageCreateInfo({}, vk::ShaderStageFlagBits::eFragment,
                                        *fragMod, "main")};
  vk::PipelineVertexInputStateCreateInfo emptyVI{};
  vk::PipelineInputAssemblyStateCreateInfo iaCI(
      {}, vk::PrimitiveTopology::eTriangleList);
  vk::PipelineRasterizationStateCreateInfo rasCI(
      {}, false, false, vk::PolygonMode::eFill, vk::CullModeFlagBits::eNone,
      vk::FrontFace::eCounterClockwise, false, 0, 0, 0, 1.f);
  vk::PipelineColorBlendAttachmentState blendAtt(
      false, {}, {}, {}, {}, {}, {},
      vk::ColorComponentFlagBits::eR | vk::ColorComponentFlagBits::eG |
          vk::ColorComponentFlagBits::eB | vk::ColorComponentFlagBits::eA);
  vk::PipelineColorBlendStateCreateInfo cbCI({}, false, {}, blendAtt);
  vk::PipelineDepthStencilStateCreateInfo dsCI(
      {}, false, false, vk::CompareOp::eLessOrEqual);
  vk::PipelineViewportStateCreateInfo vpCI({}, 1, nullptr, 1, nullptr);
  vk::PipelineMultisampleStateCreateInfo msCI({}, vk::SampleCountFlagBits::e1);
  std::array<vk::DynamicState, 2> dynStates{vk::DynamicState::eViewport,
                                            vk::DynamicState::eScissor};
  vk::PipelineDynamicStateCreateInfo dynCI({}, dynStates);
  auto capturePipeline = vk::raii::Pipeline(
      device, pipelineCache,
      vk::GraphicsPipelineCreateInfo({}, stages, &emptyVI, &iaCI, nullptr,
                                     &vpCI, &rasCI, &msCI, &dsCI, &cbCI,
                                     &dynCI, *capturePipelineLayout,
                                     *captureRenderPass));

  std::vector<glm::mat4> rotMatrices = {
      glm::rotate(glm::rotate(glm::mat4(1.f), glm::radians(90.f),
                              glm::vec3(0,1,0)), glm::radians(180.f), glm::vec3(1,0,0)),
      glm::rotate(glm::rotate(glm::mat4(1.f), glm::radians(-90.f),
                              glm::vec3(0,1,0)), glm::radians(180.f), glm::vec3(1,0,0)),
      glm::rotate(glm::mat4(1.f), glm::radians(-90.f), glm::vec3(1,0,0)),
      glm::rotate(glm::mat4(1.f), glm::radians(90.f),  glm::vec3(1,0,0)),
      glm::rotate(glm::mat4(1.f), glm::radians(180.f), glm::vec3(1,0,0)),
      glm::rotate(glm::mat4(1.f), glm::radians(180.f), glm::vec3(0,0,1)),
  };
  glm::mat4 captureProj =
      glm::perspective(glm::radians(90.f), 1.f, 0.1f, 512.f);

  struct PrefilterPush { float roughness; uint32_t numSamples; };

  vgeu::oneTimeSubmit(
      device, commandPool, queue, [&](const vk::raii::CommandBuffer& cmd) {
        vgeu::setImageLayout(
            cmd, prefilteredMap->getImage(), fmt,
            vk::ImageSubresourceRange{vk::ImageAspectFlagBits::eColor, 0,
                                      numMips, 0, 6},
            vk::ImageLayout::eUndefined,
            vk::ImageLayout::eTransferDstOptimal);
        vgeu::setImageLayout(cmd, offscreenImg->getImage(), fmt, 0, 1,
                             vk::ImageLayout::eUndefined,
                             vk::ImageLayout::eColorAttachmentOptimal);

        vk::ClearValue clearVal;
        clearVal.color = vk::ClearColorValue(0.f, 0.f, 0.f, 1.f);

        for (uint32_t m = 0; m < numMips; ++m) {
          uint32_t mipDim = static_cast<uint32_t>(dim * std::pow(0.5f, m));
          PrefilterPush push{static_cast<float>(m) /
                                 static_cast<float>(numMips - 1),
                             32u};

          for (uint32_t f = 0; f < 6; ++f) {
            CaptureUbo uboData;
            uboData.mvp = captureProj * rotMatrices[f];
            std::memcpy(captureUboBuf->getMappedData(), &uboData,
                        sizeof(CaptureUbo));

            cmd.beginRenderPass(
                vk::RenderPassBeginInfo(
                    *captureRenderPass, *captureFBO,
                    vk::Rect2D({}, vk::Extent2D{dim, dim}), clearVal),
                vk::SubpassContents::eInline);
            cmd.setViewport(
                0, vk::Viewport(0, 0, (float)mipDim, (float)mipDim, 0.f, 1.f));
            cmd.setScissor(0, vk::Rect2D({}, vk::Extent2D{mipDim, mipDim}));
            cmd.bindPipeline(vk::PipelineBindPoint::eGraphics, *capturePipeline);
            cmd.bindDescriptorSets(vk::PipelineBindPoint::eGraphics,
                                   *capturePipelineLayout, 0,
                                   {*captureUboDS, *envDS}, nullptr);
            cmd.pushConstants<PrefilterPush>(*capturePipelineLayout,
                                             vk::ShaderStageFlagBits::eFragment,
                                             0, push);
            cmd.draw(36, 1, 0, 0);
            cmd.endRenderPass();

            vgeu::setImageLayout(cmd, offscreenImg->getImage(), fmt, 0, 1,
                                 vk::ImageLayout::eColorAttachmentOptimal,
                                 vk::ImageLayout::eTransferSrcOptimal);
            vk::ImageCopy copyRegion(
                vk::ImageSubresourceLayers{vk::ImageAspectFlagBits::eColor, 0,
                                          0, 1},
                {},
                vk::ImageSubresourceLayers{vk::ImageAspectFlagBits::eColor, m,
                                          f, 1},
                {}, vk::Extent3D{mipDim, mipDim, 1});
            cmd.copyImage(offscreenImg->getImage(),
                          vk::ImageLayout::eTransferSrcOptimal,
                          prefilteredMap->getImage(),
                          vk::ImageLayout::eTransferDstOptimal, copyRegion);
            vgeu::setImageLayout(cmd, offscreenImg->getImage(), fmt, 0, 1,
                                 vk::ImageLayout::eTransferSrcOptimal,
                                 vk::ImageLayout::eColorAttachmentOptimal);
          }
        }

        vgeu::setImageLayout(
            cmd, prefilteredMap->getImage(), fmt,
            vk::ImageSubresourceRange{vk::ImageAspectFlagBits::eColor, 0,
                                      numMips, 0, 6},
            vk::ImageLayout::eTransferDstOptimal,
            vk::ImageLayout::eShaderReadOnlyOptimal);
      });
}
```

- [ ] **Step 3: clang-format + 빌드 확인**

```bash
clang-format -i src/examples/pbr/pbr.cpp
cmake --build ./build --config Debug 2>&1 | tail -30
```

- [ ] **Step 4: 커밋**

```bash
rtk git add src/examples/pbr/pbr.cpp
rtk git commit -m "feat(pbr): implement buildIrradianceMap and buildPrefilteredMap"
```

---

## Task 6: buildBrdfLut + prepareIBL + iblSampler 구현

**Files:**
- Modify: `src/examples/pbr/pbr.cpp`

- [ ] **Step 1: buildBrdfLut() 구현**

`buildPrefilteredMap()` 다음에 추가:

```cpp
void VgeExample::buildBrdfLut() {
  const vk::Format fmt = vk::Format::eR16G16Sfloat;
  const uint32_t dim = 512;

  brdfLut = std::make_unique<vgeu::VgeuImage>(
      device, globalAllocator->getAllocator(), fmt, vk::Extent2D{dim, dim},
      vk::ImageTiling::eOptimal,
      vk::ImageUsageFlagBits::eColorAttachment |
          vk::ImageUsageFlagBits::eSampled,
      vk::ImageLayout::eUndefined, VmaMemoryUsage::VMA_MEMORY_USAGE_AUTO,
      VmaAllocationCreateFlagBits::VMA_ALLOCATION_CREATE_DEDICATED_MEMORY_BIT,
      vk::ImageAspectFlagBits::eColor, 1);

  // RenderPass: finalLayout = SHADER_READ_ONLY_OPTIMAL (직접 최종)
  vk::AttachmentDescription attDesc(
      {}, fmt, vk::SampleCountFlagBits::e1, vk::AttachmentLoadOp::eClear,
      vk::AttachmentStoreOp::eStore, vk::AttachmentLoadOp::eDontCare,
      vk::AttachmentStoreOp::eDontCare, vk::ImageLayout::eUndefined,
      vk::ImageLayout::eShaderReadOnlyOptimal);
  vk::AttachmentReference colorRef(0, vk::ImageLayout::eColorAttachmentOptimal);
  vk::SubpassDescription subpass({}, vk::PipelineBindPoint::eGraphics, {},
                                 colorRef);
  std::array<vk::SubpassDependency, 2> deps{
      vk::SubpassDependency(VK_SUBPASS_EXTERNAL, 0,
          vk::PipelineStageFlagBits::eBottomOfPipe,
          vk::PipelineStageFlagBits::eColorAttachmentOutput,
          vk::AccessFlagBits::eMemoryRead,
          vk::AccessFlagBits::eColorAttachmentWrite,
          vk::DependencyFlagBits::eByRegion),
      vk::SubpassDependency(0, VK_SUBPASS_EXTERNAL,
          vk::PipelineStageFlagBits::eColorAttachmentOutput,
          vk::PipelineStageFlagBits::eBottomOfPipe,
          vk::AccessFlagBits::eColorAttachmentWrite,
          vk::AccessFlagBits::eMemoryRead,
          vk::DependencyFlagBits::eByRegion)};
  auto brdfRenderPass = vk::raii::RenderPass(
      device, vk::RenderPassCreateInfo({}, attDesc, subpass, deps));

  vk::ImageView brdfView = *brdfLut->getImageView();
  auto brdfFBO = vk::raii::Framebuffer(
      device,
      vk::FramebufferCreateInfo({}, *brdfRenderPass, brdfView, dim, dim, 1));

  // Empty descriptor set layout (no bindings)
  auto emptyDSL = vk::raii::DescriptorSetLayout(
      device, vk::DescriptorSetLayoutCreateInfo({}, nullptr));
  auto brdfPipelineLayout = vk::raii::PipelineLayout(
      device, vk::PipelineLayoutCreateInfo({}, *emptyDSL));

  // Pipeline (brdf_lut.vert + brdf_lut.frag)
  auto vertCode = vgeu::readFile(getShadersPath() + "/pbr/brdf_lut.vert.spv");
  auto fragCode = vgeu::readFile(getShadersPath() + "/pbr/brdf_lut.frag.spv");
  auto vertMod = vgeu::createShaderModule(device, vertCode);
  auto fragMod = vgeu::createShaderModule(device, fragCode);
  std::array<vk::PipelineShaderStageCreateInfo, 2> stages{
      vk::PipelineShaderStageCreateInfo({}, vk::ShaderStageFlagBits::eVertex,
                                        *vertMod, "main"),
      vk::PipelineShaderStageCreateInfo({}, vk::ShaderStageFlagBits::eFragment,
                                        *fragMod, "main")};
  vk::PipelineVertexInputStateCreateInfo emptyVI{};
  vk::PipelineInputAssemblyStateCreateInfo iaCI(
      {}, vk::PrimitiveTopology::eTriangleList);
  vk::PipelineRasterizationStateCreateInfo rasCI(
      {}, false, false, vk::PolygonMode::eFill, vk::CullModeFlagBits::eNone,
      vk::FrontFace::eCounterClockwise, false, 0, 0, 0, 1.f);
  // brdf_lut outputs RG (2 channels)
  vk::PipelineColorBlendAttachmentState blendAtt(
      false, {}, {}, {}, {}, {}, {},
      vk::ColorComponentFlagBits::eR | vk::ColorComponentFlagBits::eG);
  vk::PipelineColorBlendStateCreateInfo cbCI({}, false, {}, blendAtt);
  vk::PipelineDepthStencilStateCreateInfo dsCI(
      {}, false, false, vk::CompareOp::eLessOrEqual);
  vk::PipelineViewportStateCreateInfo vpCI({}, 1, nullptr, 1, nullptr);
  vk::PipelineMultisampleStateCreateInfo msCI({}, vk::SampleCountFlagBits::e1);
  std::array<vk::DynamicState, 2> dynStates{vk::DynamicState::eViewport,
                                            vk::DynamicState::eScissor};
  vk::PipelineDynamicStateCreateInfo dynCI({}, dynStates);
  auto brdfPipeline = vk::raii::Pipeline(
      device, pipelineCache,
      vk::GraphicsPipelineCreateInfo({}, stages, &emptyVI, &iaCI, nullptr,
                                     &vpCI, &rasCI, &msCI, &dsCI, &cbCI,
                                     &dynCI, *brdfPipelineLayout,
                                     *brdfRenderPass));

  // Render
  vgeu::oneTimeSubmit(
      device, commandPool, queue, [&](const vk::raii::CommandBuffer& cmd) {
        vk::ClearValue clearVal;
        clearVal.color = vk::ClearColorValue(0.f, 0.f, 0.f, 1.f);
        cmd.beginRenderPass(
            vk::RenderPassBeginInfo(*brdfRenderPass, *brdfFBO,
                                    vk::Rect2D({}, vk::Extent2D{dim, dim}),
                                    clearVal),
            vk::SubpassContents::eInline);
        cmd.setViewport(
            0, vk::Viewport(0.f, 0.f, (float)dim, (float)dim, 0.f, 1.f));
        cmd.setScissor(0, vk::Rect2D({}, vk::Extent2D{dim, dim}));
        cmd.bindPipeline(vk::PipelineBindPoint::eGraphics, *brdfPipeline);
        cmd.draw(3, 1, 0, 0);  // fullscreen triangle
        cmd.endRenderPass();
      });
}
```

- [ ] **Step 2: prepareIBL() 구현 + iblSampler 생성**

`buildBrdfLut()` 다음에 추가:

```cpp
void VgeExample::prepareIBL() {
  // IBL sampler: LINEAR/LINEAR + CLAMP_TO_EDGE, maxLod 커버
  vk::SamplerCreateInfo samplerCI(
      {}, vk::Filter::eLinear, vk::Filter::eLinear,
      vk::SamplerMipmapMode::eLinear, vk::SamplerAddressMode::eClampToEdge,
      vk::SamplerAddressMode::eClampToEdge, vk::SamplerAddressMode::eClampToEdge,
      0.f, false, 1.f, false, vk::CompareOp::eNever, 0.f,
      static_cast<float>(static_cast<uint32_t>(
          std::floor(std::log2(512))) + 1),  // maxLod = 10
      vk::BorderColor::eFloatOpaqueWhite);
  iblSampler = vk::raii::Sampler(device, samplerCI);

  loadHdrTexture();
  buildEnvCubemap();
  buildIrradianceMap();
  buildPrefilteredMap();
  buildBrdfLut();
}
```

- [ ] **Step 3: clang-format + 빌드 확인**

```bash
clang-format -i src/examples/pbr/pbr.cpp
cmake --build ./build --config Debug 2>&1 | tail -30
```

- [ ] **Step 4: 커밋**

```bash
rtk git add src/examples/pbr/pbr.cpp
rtk git commit -m "feat(pbr): implement buildBrdfLut and prepareIBL with iblSampler"
```

---

## Task 7: pbr.frag IBL ambient 수정

**Files:**
- Modify: `shaders/pbr/pbr.frag`

- [ ] **Step 1: pbr.frag — IBL 텍스처 바인딩 + FresnelSchlickRoughness 추가**

`shaders/pbr/pbr.frag`에서 `layout (set = 0, binding = 7)` 다음에 추가:

```glsl
// IBL 텍스처 (set=1)
layout (set = 1, binding = 0) uniform samplerCube irradianceMap;
layout (set = 1, binding = 1) uniform samplerCube prefilteredMap;
layout (set = 1, binding = 2) uniform sampler2D   brdfLut;
```

- [ ] **Step 2: pbr.frag — UBO에 useIBL 필드 추가**

`pbr.frag`의 UBO 구조체에서 `float _pad2;` 다음에 추가:

```glsl
    int useIBL;
    int _pad3[3];
```

- [ ] **Step 3: pbr.frag — FresnelSchlickRoughness 함수 추가**

`FresnelSchlick` 함수 다음에 추가:

```glsl
vec3 FresnelSchlickRoughness(float cosTheta, vec3 F0, float roughness) {
    return F0 + (max(vec3(1.0 - roughness), F0) - F0)
           * pow(clamp(1.0 - cosTheta, 0.0, 1.0), 5.0);
}
```

- [ ] **Step 4: pbr.frag — ambient 블록 교체**

기존 line 142:
```glsl
    vec3 ambient = vec3(ubo.ambientStrength) * albedo * ao;
```
를 다음으로 교체:

```glsl
    vec3 ambient;
    if (ubo.useIBL != 0) {
        vec3 F  = FresnelSchlickRoughness(max(dot(N, V), 0.0), F0, roughness);
        vec3 kD = (1.0 - F) * (1.0 - metallic);

        vec3 irradiance = texture(irradianceMap, N).rgb;
        vec3 diffuse    = irradiance * albedo;

        const float MAX_LOD = 9.0;
        vec3 R = reflect(-V, N);
        vec3 prefilteredColor =
            textureLod(prefilteredMap, R, roughness * MAX_LOD).rgb;
        vec2 envBRDF =
            texture(brdfLut, vec2(max(dot(N, V), 0.0), roughness)).rg;
        vec3 specular = prefilteredColor * (F * envBRDF.x + envBRDF.y);

        ambient = (kD * diffuse + specular) * ao;
    } else {
        ambient = vec3(ubo.ambientStrength) * albedo * ao;
    }
```

- [ ] **Step 5: pbr.frag.spv 재컴파일**

```bash
glslc shaders/pbr/pbr.frag -o shaders/pbr/pbr.frag.spv
```

Expected: 에러 없이 컴파일됨.

- [ ] **Step 6: 커밋**

```bash
rtk git add shaders/pbr/pbr.frag shaders/pbr/pbr.frag.spv
rtk git commit -m "feat(shaders/pbr): add IBL ambient to pbr.frag (set=1 + useIBL branch)"
```

---

## Task 8: IBL descriptor sets + pipelineLayoutComposition 수정

**Files:**
- Modify: `src/examples/pbr/pbr.cpp`
- Modify: `src/examples/pbr/pbr.hpp` (이미 선언됨, 구현만 추가)

- [ ] **Step 1: setupDescriptors()에 IBL descriptor set layout/sets 추가**

`setupDescriptors()`의 descriptor pool 크기 계산부에 IBL 샘플러 3개 추가:

```cpp
  // 기존 poolSizes 선언 후, eCombinedImageSampler 항목의 count에 추가:
  // 기존: offScreenFrameBuf.numAttachments * MAX_CONCURRENT_FRAMES + 4 + 2
  // 변경: + 3 * MAX_CONCURRENT_FRAMES (IBL: irradiance, prefilter, brdfLut)
  //       + MAX_CONCURRENT_FRAMES     (skybox: envCubemap)
```

실제 코드: `pbr.cpp` setupDescriptors()의 poolSizes 정의에서:

```cpp
  poolSizes.emplace_back(
      vk::DescriptorType::eCombinedImageSampler,
      static_cast<uint32_t>(MAX_CONCURRENT_FRAMES *
                            offScreenFrameBuf.numAttachments) +
          4u /*sphere dummy*/ + 2u /*height map*/ +
          3u * MAX_CONCURRENT_FRAMES /*IBL textures*/ +
          1u * MAX_CONCURRENT_FRAMES /*skybox envCubemap*/);
```

descriptor pool max sets에도 추가:

```cpp
  vk::DescriptorPoolCreateInfo poolCI(
      vk::DescriptorPoolCreateFlagBits::eFreeDescriptorSet,
      MAX_CONCURRENT_FRAMES /*composition*/ +
          MAX_CONCURRENT_FRAMES * 2 /*offscreen + dynamic*/ +
          MAX_CONCURRENT_FRAMES /*sprite*/ + 1u /*sphere dummy*/ +
          2u /*height map*/ +
          MAX_CONCURRENT_FRAMES /*IBL*/ +
          MAX_CONCURRENT_FRAMES /*skybox*/,
      poolSizes);
```

- [ ] **Step 2: IBL descriptor set layout 생성**

`setupDescriptors()` 내에서 `spriteLightDescriptorSetLayout` 생성 다음에 추가:

```cpp
  // IBL descriptor set layout: set=1 in pbr.frag
  // binding 0: irradianceMap, 1: prefilteredMap, 2: brdfLut
  {
    std::vector<vk::DescriptorSetLayoutBinding> iblBindings;
    iblBindings.emplace_back(0, vk::DescriptorType::eCombinedImageSampler, 1,
                             vk::ShaderStageFlagBits::eFragment);
    iblBindings.emplace_back(1, vk::DescriptorType::eCombinedImageSampler, 1,
                             vk::ShaderStageFlagBits::eFragment);
    iblBindings.emplace_back(2, vk::DescriptorType::eCombinedImageSampler, 1,
                             vk::ShaderStageFlagBits::eFragment);
    iblDescriptorSetLayout = vk::raii::DescriptorSetLayout(
        device, vk::DescriptorSetLayoutCreateInfo({}, iblBindings));
  }
```

- [ ] **Step 3: pipelineLayoutComposition을 set=0+set=1로 수정**

`setupDescriptors()`의 composition descriptor set layout 생성 블록에서:

```cpp
    // 기존:
    pipelineLayoutComposition = vk::raii::PipelineLayout(
        device,
        vk::PipelineLayoutCreateInfo({}, *compositionDescriptorSetLayout));

    // 변경: IBL set 포함
    std::vector<vk::DescriptorSetLayout> compLayouts = {
        *compositionDescriptorSetLayout, *iblDescriptorSetLayout};
    pipelineLayoutComposition = vk::raii::PipelineLayout(
        device, vk::PipelineLayoutCreateInfo({}, compLayouts));
```

주의: `iblDescriptorSetLayout`은 위 Step 2에서 생성하므로 순서 중요. 이 블록을 `iblDescriptorSetLayout` 생성 다음으로 이동하거나, Step 2의 코드를 composition layout 생성 전에 배치.

- [ ] **Step 4: IBL descriptor sets 할당 + write**

setupDescriptors()의 마지막 `height map descriptor sets` 블록 다음에 추가:

```cpp
  // IBL descriptor sets (composition set=1)
  {
    vk::DescriptorSetAllocateInfo allocInfo(*descriptorPool,
                                            *iblDescriptorSetLayout);
    iblDescriptorSets.reserve(MAX_CONCURRENT_FRAMES);
    for (int i = 0; i < MAX_CONCURRENT_FRAMES; i++)
      iblDescriptorSets.push_back(
          std::move(vk::raii::DescriptorSets(device, allocInfo).front()));

    auto irrInfo = irradianceMap->descriptorImageInfo(
        *iblSampler, vk::ImageLayout::eShaderReadOnlyOptimal);
    auto preInfo = prefilteredMap->descriptorImageInfo(
        *iblSampler, vk::ImageLayout::eShaderReadOnlyOptimal);
    auto lutInfo = brdfLut->descriptorImageInfo(
        *iblSampler, vk::ImageLayout::eShaderReadOnlyOptimal);

    for (int i = 0; i < MAX_CONCURRENT_FRAMES; i++) {
      std::vector<vk::WriteDescriptorSet> writes;
      writes.emplace_back(*iblDescriptorSets[i], 0, 0,
                          vk::DescriptorType::eCombinedImageSampler, irrInfo,
                          nullptr);
      writes.emplace_back(*iblDescriptorSets[i], 1, 0,
                          vk::DescriptorType::eCombinedImageSampler, preInfo,
                          nullptr);
      writes.emplace_back(*iblDescriptorSets[i], 2, 0,
                          vk::DescriptorType::eCombinedImageSampler, lutInfo,
                          nullptr);
      device.updateDescriptorSets(writes, nullptr);
    }
  }
```

- [ ] **Step 5: updateUboComposition()에 useIBL 업데이트 추가**

`updateUboComposition()`에서 `ambientStrength` 업데이트 다음에:

```cpp
  uniformDataComposition.useIBL = opts.useIBL ? 1 : 0;
```

- [ ] **Step 6: clang-format + 빌드 확인**

```bash
clang-format -i src/examples/pbr/pbr.cpp
cmake --build ./build --config Debug 2>&1 | tail -30
```

- [ ] **Step 7: 커밋**

```bash
rtk git add src/examples/pbr/pbr.cpp
rtk git commit -m "feat(pbr): add IBL descriptor sets and update pipelineLayoutComposition"
```

---

## Task 9: Skybox 셰이더 + 파이프라인 + 디스크립터

**Files:**
- Create: `shaders/pbr/skybox.vert`
- Create: `shaders/pbr/skybox.frag`
- Modify: `src/examples/pbr/pbr.cpp`

- [ ] **Step 1: skybox.vert 작성**

```glsl
// shaders/pbr/skybox.vert
#version 450

layout(push_constant) uniform PushBlock {
    mat4 view;
    mat4 projection;
} push;

layout(location = 0) out vec3 outUVW;

const vec3 positions[36] = vec3[](
    vec3( 1,-1, 1),vec3( 1,-1,-1),vec3( 1, 1,-1),
    vec3( 1, 1,-1),vec3( 1, 1, 1),vec3( 1,-1, 1),
    vec3(-1,-1,-1),vec3(-1,-1, 1),vec3(-1, 1, 1),
    vec3(-1, 1, 1),vec3(-1, 1,-1),vec3(-1,-1,-1),
    vec3(-1, 1, 1),vec3( 1, 1, 1),vec3( 1, 1,-1),
    vec3( 1, 1,-1),vec3(-1, 1,-1),vec3(-1, 1, 1),
    vec3(-1,-1,-1),vec3( 1,-1,-1),vec3( 1,-1, 1),
    vec3( 1,-1, 1),vec3(-1,-1, 1),vec3(-1,-1,-1),
    vec3(-1,-1, 1),vec3(-1, 1, 1),vec3( 1, 1, 1),
    vec3( 1, 1, 1),vec3( 1,-1, 1),vec3(-1,-1, 1),
    vec3( 1,-1,-1),vec3( 1, 1,-1),vec3(-1, 1,-1),
    vec3(-1, 1,-1),vec3(-1,-1,-1),vec3( 1,-1,-1)
);

void main() {
    outUVW = positions[gl_VertexIndex];
    // xyww: depth = w/w = 1.0 (max depth, renders behind everything)
    gl_Position =
        (push.projection * push.view * vec4(outUVW, 1.0)).xyww;
}
```

- [ ] **Step 2: skybox.frag 작성**

```glsl
// shaders/pbr/skybox.frag
#version 450

layout(set = 0, binding = 0) uniform samplerCube envMap;

layout(push_constant) uniform PushBlock {
    mat4 view;
    mat4 projection;
} push;

layout(location = 0) in  vec3 inUVW;
layout(location = 0) out vec4 outColor;

vec3 Uncharted2Tonemap(vec3 color) {
    float A = 0.15, B = 0.50, C = 0.10, D = 0.20,
          E = 0.02, F = 0.30, W = 11.2;
    return ((color*(A*color+C*B)+D*E)/(color*(A*color+B)+D*F))-E/F;
}

void main() {
    vec3 color = texture(envMap, inUVW).rgb;
    // Uncharted2 tonemap (exposure=4.5)
    color = Uncharted2Tonemap(color * 4.5);
    color = color * (1.0 / Uncharted2Tonemap(vec3(11.2)));
    // Gamma correction (gamma=2.2)
    color = pow(color, vec3(1.0 / 2.2));
    outColor = vec4(color, 1.0);
}
```

- [ ] **Step 3: 셰이더 컴파일**

```bash
glslc shaders/pbr/skybox.vert -o shaders/pbr/skybox.vert.spv
glslc shaders/pbr/skybox.frag -o shaders/pbr/skybox.frag.spv
```

- [ ] **Step 4: prepareSkyboxDescriptors() 구현**

`pbr.cpp`에서 `setupDescriptors()` 함수 뒤 (또는 IBL descriptor 추가 블록 다음)에 추가. 실제로는 setupDescriptors() 내 마지막에 삽입:

```cpp
  // Skybox descriptor set layout: binding=0 samplerCube
  {
    vk::DescriptorSetLayoutBinding envBinding(
        0, vk::DescriptorType::eCombinedImageSampler, 1,
        vk::ShaderStageFlagBits::eFragment);
    skyboxDescriptorSetLayout = vk::raii::DescriptorSetLayout(
        device, vk::DescriptorSetLayoutCreateInfo({}, envBinding));
  }

  // Skybox descriptor sets
  {
    vk::DescriptorSetAllocateInfo allocInfo(*descriptorPool,
                                            *skyboxDescriptorSetLayout);
    skyboxDescriptorSets.reserve(MAX_CONCURRENT_FRAMES);
    for (int i = 0; i < MAX_CONCURRENT_FRAMES; i++)
      skyboxDescriptorSets.push_back(
          std::move(vk::raii::DescriptorSets(device, allocInfo).front()));

    auto envInfo = envCubemap->descriptorImageInfo(
        *iblSampler, vk::ImageLayout::eShaderReadOnlyOptimal);
    for (int i = 0; i < MAX_CONCURRENT_FRAMES; i++) {
      device.updateDescriptorSets(
          vk::WriteDescriptorSet(*skyboxDescriptorSets[i], 0, 0,
                                 vk::DescriptorType::eCombinedImageSampler,
                                 envInfo, nullptr),
          nullptr);
    }
  }
```

- [ ] **Step 5: prepareSkyboxPipeline() 구현**

`preparePipelines()` 내 기존 sprite 파이프라인 다음에 추가:

```cpp
  // --- Skybox pipeline ---
  {
    auto vertCode = vgeu::readFile(getShadersPath() + "/pbr/skybox.vert.spv");
    auto fragCode = vgeu::readFile(getShadersPath() + "/pbr/skybox.frag.spv");
    auto vertModule = vgeu::createShaderModule(device, vertCode);
    auto fragModule = vgeu::createShaderModule(device, fragCode);

    std::array<vk::PipelineShaderStageCreateInfo, 2> stages{
        vk::PipelineShaderStageCreateInfo({}, vk::ShaderStageFlagBits::eVertex,
                                          *vertModule, "main"),
        vk::PipelineShaderStageCreateInfo({},
                                          vk::ShaderStageFlagBits::eFragment,
                                          *fragModule, "main")};

    vk::PipelineVertexInputStateCreateInfo emptyVertexSCI{};
    // front face cull: 큐브 안쪽을 본다
    rasterizationSCI.cullMode = vk::CullModeFlagBits::eFront;

    // depth test ON, depth write OFF, compareOp = LessOrEqual
    // skybox.vert의 .xyww 트릭으로 depth=1.0 → 씬 오브젝트 뒤에 렌더됨
    vk::PipelineDepthStencilStateCreateInfo skyboxDepthSCI(
        {}, true /*depthTest*/, false /*depthWrite*/,
        vk::CompareOp::eLessOrEqual);

    colorBlendSCI.setAttachments(blendAttachment);  // no blend

    // Push constant: view(64) + projection(64) = 128 bytes
    vk::PushConstantRange pcRange(
        vk::ShaderStageFlagBits::eVertex | vk::ShaderStageFlagBits::eFragment,
        0, sizeof(SkyboxPushConstants));
    skyboxPipelineLayout = vk::raii::PipelineLayout(
        device, vk::PipelineLayoutCreateInfo({}, *skyboxDescriptorSetLayout,
                                              pcRange));

    vk::GraphicsPipelineCreateInfo pipelineCI(
        {}, stages, &emptyVertexSCI, &inputAssemblySCI, nullptr, &viewportSCI,
        &rasterizationSCI, &multisampleSCI, &skyboxDepthSCI, &colorBlendSCI,
        &dynamicSCI, *skyboxPipelineLayout, *renderPass);
    skyboxPipeline = vk::raii::Pipeline(device, pipelineCache, pipelineCI);
  }
```

- [ ] **Step 6: clang-format + 빌드 확인**

```bash
clang-format -i src/examples/pbr/pbr.cpp
cmake --build ./build --config Debug 2>&1 | tail -30
```

- [ ] **Step 7: 커밋**

```bash
rtk git add shaders/pbr/skybox.vert shaders/pbr/skybox.frag \
            shaders/pbr/skybox.vert.spv shaders/pbr/skybox.frag.spv \
            src/examples/pbr/pbr.cpp
rtk git commit -m "feat(pbr): add skybox shaders, pipeline, and descriptor sets"
```

---

## Task 10: 통합 — prepare() / buildCommandBuffers() / UI

**Files:**
- Modify: `src/examples/pbr/pbr.cpp`

- [ ] **Step 1: prepare()에 prepareIBL() 추가**

`pbr.cpp`의 `prepare()` 함수에서 `prepareUniformBuffers()` 다음에 추가:

```cpp
  prepareIBL();  // HDR 로딩 + IBL 텍스처 4종 생성 (startup 1회)
```

최종 순서:
```cpp
void VgeExample::prepare() {
  VgeBase::prepare();
  loadAssets();
  setupDynamicUbo();
  prepareOffScreenFrameBuffer();
  prepareUniformBuffers();
  prepareIBL();          // ← 신규
  setupDescriptors();
  preparePipelines();
  prepared = true;
}
```

- [ ] **Step 2: buildCommandBuffers()에 skybox draw 추가**

`buildCommandBuffers()`에서 composition draw (`cmd.draw(3, 1, 0, 0)`) 다음, sprite pass 이전에 추가:

```cpp
    // --- Skybox pass (useIBL 시에만) ---
    if (opts.useIBL) {
      // view matrix: translation 제거 (mat3 → mat4)
      glm::mat4 skyboxView =
          glm::mat4(glm::mat3(camera.getView()));
      SkyboxPushConstants skyboxPC{skyboxView,
                                    camera.getProjection()};

      cmd.bindPipeline(vk::PipelineBindPoint::eGraphics, *skyboxPipeline);
      cmd.bindDescriptorSets(
          vk::PipelineBindPoint::eGraphics, *skyboxPipelineLayout, 0,
          {*skyboxDescriptorSets[currentFrameIndex]}, nullptr);
      cmd.pushConstants<SkyboxPushConstants>(
          *skyboxPipelineLayout,
          vk::ShaderStageFlagBits::eVertex | vk::ShaderStageFlagBits::eFragment,
          0, skyboxPC);
      cmd.draw(36, 1, 0, 0);

      // composition descriptor set 재바인딩
      // (skybox pipeline layout이 달라서 VUID-02697 방지)
      cmd.bindDescriptorSets(
          vk::PipelineBindPoint::eGraphics, *pipelineLayoutComposition, 0,
          {*descriptorSets.composition[currentFrameIndex]}, nullptr);
      cmd.bindDescriptorSets(
          vk::PipelineBindPoint::eGraphics, *pipelineLayoutComposition, 1,
          {*iblDescriptorSets[currentFrameIndex]}, nullptr);
    }
```

- [ ] **Step 3: composition draw 시 IBL descriptor set 바인딩 추가**

기존 composition draw:
```cpp
    cmd.bindPipeline(vk::PipelineBindPoint::eGraphics, *pipelines.composition);
    cmd.bindDescriptorSets(
        vk::PipelineBindPoint::eGraphics, *pipelineLayoutComposition, 0,
        {*descriptorSets.composition[currentFrameIndex]}, nullptr);
    cmd.draw(3, 1, 0, 0);
```

를 다음으로 변경:
```cpp
    cmd.bindPipeline(vk::PipelineBindPoint::eGraphics, *pipelines.composition);
    cmd.bindDescriptorSets(
        vk::PipelineBindPoint::eGraphics, *pipelineLayoutComposition, 0,
        {*descriptorSets.composition[currentFrameIndex]}, nullptr);
    cmd.bindDescriptorSets(
        vk::PipelineBindPoint::eGraphics, *pipelineLayoutComposition, 1,
        {*iblDescriptorSets[currentFrameIndex]}, nullptr);
    cmd.draw(3, 1, 0, 0);
```

- [ ] **Step 4: displayTargets 루프 전 IBL descriptor set도 재바인딩**

기존 rebind composition set 블록 다음에 IBL set도 추가:

```cpp
    cmd.bindDescriptorSets(
        vk::PipelineBindPoint::eGraphics, *pipelineLayoutComposition, 0,
        {*descriptorSets.composition[currentFrameIndex]}, nullptr);
    // IBL set=1 재바인딩 (displayTargets는 set=1 미사용이지만 안전하게)
    cmd.bindDescriptorSets(
        vk::PipelineBindPoint::eGraphics, *pipelineLayoutComposition, 1,
        {*iblDescriptorSets[currentFrameIndex]}, nullptr);
```

- [ ] **Step 5: onUpdateUIOverlay()에 IBL 토글 추가**

`onUpdateUIOverlay()`의 `ImGui::Separator();` (Directional Light 위) 전에 추가:

```cpp
      ImGui::Separator();
      ImGui::Checkbox("Use IBL", &opts.useIBL);
      if (opts.useIBL) {
        ImGui::DragFloat("IBL Exposure", &opts.iblExposure, 0.1f, 0.1f, 10.f,
                         "%.1f");
      }
```

- [ ] **Step 6: clang-format 적용**

```bash
clang-format -i src/examples/pbr/pbr.cpp
```

- [ ] **Step 7: 최종 빌드 확인**

```bash
cmake --build ./build --config Debug 2>&1 | tail -30
```

Expected: 에러 없이 빌드 성공.

- [ ] **Step 8: 런타임 검증**

앱 실행 후:
1. IBL off 상태에서 기존 렌더링과 동일한지 확인
2. UI에서 "Use IBL" 체크박스 활성화
3. 씬에 ambient lighting이 환경맵 기반으로 바뀌는지 확인 (구름/나무 색조)
4. 배경에 tree-lined driveway skybox가 보이는지 확인
5. Vulkan validation 에러 없는지 확인

- [ ] **Step 9: 커밋**

```bash
rtk git add src/examples/pbr/pbr.cpp
rtk git commit -m "feat(pbr): wire up IBL in prepare/buildCommandBuffers, add UI toggle"
```

---

## Task 11: 스펙 커버리지 확인 + 문서 업데이트

- [ ] **Step 1: 스펙 요구사항 점검**

| 요구사항 | 구현 Task |
|----------|-----------|
| HDR equirectangular → envCubemap | Task 4 |
| irradiance map (64×64, full mip) | Task 5 |
| pre-filtered map (512×512, 10 mip) | Task 5 |
| BRDF LUT (512×512, RG16F) | Task 6 |
| IBL ambient in pbr.frag | Task 7 |
| IBL descriptor set (composition set=1) | Task 8 |
| Skybox pass (envCubemap 배경) | Task 9 |
| useIBL 단일 토글 | Task 10 |
| VgeuImage cubemap 지원 | Task 1 |

- [ ] **Step 2: 최종 커밋**

```bash
rtk git add docs/superpowers/plans/2026-04-08-ibl.md
rtk git commit -m "docs(plans): add IBL implementation plan"
```
